# assert-unreferenced-strings baseline (S1568).
# Names measured as unreferenced that are deliberately KEPT. One per line, with the reason.
# Regenerate the measurement with: pwsh -NoProfile -File scripts/utils/audit-unreferenced-strings.ps1 -Module app_v2 -File strings.xml
# A new name here needs a written reason - an unexplained entry is how the previous 397 accumulated.
passthrough_capture_error  # same feature
passthrough_capture_error_save  # same feature
passthrough_capture_permission_denied  # same feature
passthrough_capture_permission_needed  # same feature
passthrough_capture_saved  # VR passthrough capture is contracted but unimplemented - see reason below
passthrough_capture_timeout  # same feature
passthrough_capture_unavailable  # same feature
share_target_desc_package_app  # held by S2390 until its device verdict - delete when that ticket closes
splash_slogan  # TODO: state why this name is kept
tourist_daylight_active  # held by S2922 - Day/Night badge has no layout slot until the Rule 10 placement is decided
tourist_daylight_night  # held by S2922 - see tourist_daylight_active
tourist_status_permission_required  # held by S2922 - the permission flags it captions are never written yet
wear_sync_now  # held by S2484, phases 04-05 - delete this line when that ticket closes
wear_sync_now_description  # held by S2484, same two phases
