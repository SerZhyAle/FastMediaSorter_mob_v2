# blockneedusertest-probe-baseline.txt (S1290)
#
# Allow-list for the reverse direction of the debug-probe invariant, read by
# scripts/quality/assert-no-ticket-logs.ps1.
#
# CLAUDE.md Rule 2 states the invariant as "probe exists in .kt if and only if the ticket is in
# BlockNeedUserTest". Measured 2026-08-14, the literal form is too strong in one direction: a ticket
# that changes tooling, scripts or documentation and touches no Kotlin has nowhere to put a probe,
# yet still needs a human to verify it on the real thing. Those tickets are excused here, by id,
# with the reason - never by a bare counter, so the number can only move with an explanation
# (strategic S1290 ADR-1).
#
# Format: one record per line, "Sxxxx  <reason>". Lines starting with # are comments.
# An entry stops being read the moment its ticket leaves BlockNeedUserTest, so stale rows are inert
# rather than harmful - but delete them when you notice them.
#
# NOT a place to park a ticket that genuinely should carry a probe. If the ticket changed Kotlin,
# add the probe instead.

S3076  R8 keep rule only (app_v2/proguard-rules.pro), no .kt touched; debug builds are not minified, so a probe could not observe the rule - a release build restoring the focused tourist tile is the evidence.
S1606  Durable-evidence closing gate - script-only change, no .kt touched (per its own status note).
S2798  Script-only fix: set-android-string.ps1 and seed-locale-tranche.ps1 regex patterns. Verification: run with apostrophe values and inspect XML output.
S1620  Archived-spec relocation - tooling-only change, no .kt touched (per its own status note).
S1183  adb.ps1 verb output made pipeable - script-only change; the piped files and command output are the evidence.
S1520  devtest screenshot gate on secure screens - script-only change; the SHOT/TREE output is the evidence.
S1673  Maestro launcher-mode false red - touches only maestro/run-tests.ps1 and maestro/_shared/go_home.yaml; the suite verdict and the runner's launcher-mode header line are the evidence (per its own status note).
S1706  Startup splash - resources, generator and docs only, no .kt touched. The splash window is drawn by the system before the first line of app code runs, so no Timber line could observe it even if one existed; the evidence is a screen recording of a cold start (per its own status note).
S1721  Wear activity recreated on rotation - the fix is a configChanges attribute in wear/src/main/AndroidManifest.xml and no .kt changed; the evidence is that playback survives a wrist rotation, which no log line could state more directly.
S1666  Bundled owner credentials removed - the change is a deletion (a resources file, a bundled private key, an importer entry point and a BuildConfig field); there is no new flow to instrument, and the device check is the absence of the trigger plus the surviving import-from-URI path.
S1709  Wear audio-player CPU cost - the ticket is gated on a four-point measurement on a real Galaxy Watch before any fix is written, so no Kotlin has changed and no probe can exist yet; the evidence is the measurement itself (per its own status note). Delete this row when the measurement lands and the fix touches .kt.
S1744  Default section label renamed from "everything else" to "main" - the change is one string key plus the constant that names it, and its evidence is the caption on the desktop itself. S1743 is in the same state and its probe already logs the resolved section title on every bind, so a second probe would print the same value under a different id.
S1771  Renames one string value in three locales - there is no changed code flow for a probe to sit at; the check is reading the Start menu.
S1784  Removes an override rather than adding a flow - the settings screen simply stops declaring itself sensitive, so there is no new code path a probe could sit in. The evidence is the screenshot itself: it either shows the screen or it is black, which no log line could state more directly.
S1879  adb tap-by-resource-id - the ticket changes zero .kt files (a devtest PowerShell verb plus its matcher tests), so there is no app flow for a probe to sit in; the evidence is the TAP-ID line the verb itself prints and the row opening (per its own status note).
S1895  Resource, script and documentation only - colour values, theme attributes, two launcher layouts and a contrast gate. No Kotlin flow changed, so there is no entry point to carry a probe; the owner verifies by eye on a finished build (strategic S1895 §3.3).
S1912  Repository tooling only - the gate, its caller and a test harness, no Kotlin in the change. The open check is not on a device: the new advisory branch fires only on a Kotlin/Mixed closure that coincides with another ticket sitting in BlockNeedUserTest without its probe, so it is observed on the next such run rather than provoked artificially.
S1677  Watch face package - the artifact is declared android:hasCode="false" and verified to carry zero .dex, so it cannot execute a logging call at all; a probe could only go into the wear module this ticket never changed. The XSD validator verdict, the memory-footprint numbers and the measured 3.73% ambient lit-pixel figure are the evidence (per its own status note and ADR-3).
S1984  Wear pre-release driver and its content gate - the change is PowerShell (wear-prerelease-prepare.ps1, wear-prerelease-walk.ps1, assert-prerelease-content-gates.ps1), one command file and one readiness doc; strategic 1 states outright that the wear module itself does not change, and no .kt in either module mentions the ticket. The evidence is the PASS/FAIL verdict the wear pre-release walk prints against a connected watch, not a line in app code.
S2016  Launcher taskbar tray grouping - the change wraps six existing SettingsToggleRow ids in an indented LinearLayout in dialog_launcher_settings.xml and its layout-land counterpart. The spec rules out functional isEnabled gating as a non-goal, so no Kotlin flow changed and no entry point can carry a probe; the owner verifies by opening Launcher Settings, TaskBar group, and reading whether the six composition rows sit indented under "Show status tray" in portrait and in landscape.
S2328  Caption and value proximity - the change is two style blocks in themes.xml, five layout files, one PowerShell rule plus its baseline, and one ARCHITECTURE.md section. Not one line of Kotlin, so there is no entry point a probe could sit in; the row geometry is decided by the resource compiler before any code runs. The open checks are the four strategic acceptance criteria, all of them "read the row on a wide or landscape screen" - the seven network monitor screens, the two statistics rows, the camera and slideshow dialog rows, and the scheduled tasks widget stretched horizontally.
S2020  Weather attribution moved off the widget - two layouts changed (the attribution TextView leaves gadget_launcher_weather.xml and a reused string joins dialog_launcher_weather_location.xml) plus a KDoc-only sentence in OpenMeteoWeatherProvider.kt recording where the CC-BY-4.0 credit now renders. A KDoc edit changes no executable path, so a probe would sit in a flow this ticket never touched; the check is that the desktop widget shows no attribution line and the city picker does.
S2334  Caption and value proximity in the six layouts S2328's survey missed - the change is seven XML layout files plus the caption-value-split rule baseline, and not one line of Kotlin. Row geometry is decided by the resource compiler before any code runs, so there is no entry point a probe could sit in; this is the same reason S2328 carries a row here, and this ticket is only the remainder its lexical layout survey did not find. The open checks are all "read the row on a landscape screen" - the three cloud provider status rows, the colour picker's current-colour row, the EPUB font-size stepper, and the two translation language rows under Settings -> Other.
S2406  Dev monitor page - scripts, a browser page under temp/monitor/ and docs only, no .kt touched. The page's own header (fresh / writer silent / writer stopped) and the tick counter in snapshot.js are the evidence; the check is opening temp/monitor/index.html in Chrome, Edge and Firefox and watching it refresh every 3 s without a reload.
S2700  Development monitor parallel-work signals - canon PowerShell collector, terminal renderer, browser shell, tests and docs only, no .kt touched. The evidence is the shared snapshot contract plus the owner opening temp/monitor/index.html and reading the first-screen health summary.
S2396  Maestro suite flakiness and selection hardening - the change is Maestro flow YAML, one quality gate, the suite runner and the pre-release verdict aggregator, plus four documents. Not one line of Kotlin, so there is no app entry point a probe could sit in; nothing in the app was touched and the suite runs against a build this ticket did not change. The open check is behavioural and only a device shows it: three consecutive -Suite all runs on one standard-debug build must produce the same failure set.
S2527  Wear fallback icon palette - seven shared colour resources change, with no Kotlin flow or entry point to instrument; the evidence is a visual check of type-derived fallback icons without thumbnails in both file lists.
S2364  ProGuard rules, quality gate script and documentation only - no Kotlin flow changed, so there is no app entry point for a probe. Verification on release R8 build required.
S2378  Six string resources changed in EN/RU/UK only, with no Kotlin entry point to instrument; evidence is the limited-access browser text on a phone/emulator.
S1902  Option D declined adding privileged APIs (Device Admin / Accessibility Service) for standard flavor. Policy decision with no Kotlin code flow changed.
S2565  Deletes two orphaned wear splash resources and touches no Kotlin, so there is no changed flow to instrument. The splash window is drawn by the platform before the first line of app code runs - the same reason S1706 is on this list - so no Timber line could observe it; the device check is a cold start on the watch showing the launcher icon and no crash.
S1270  Splits one native translation unit into four (.cpp/.h plus CMakeLists and a gate baseline) and touches no Kotlin, so there is nowhere to put a Timber line. The changed code is the OpenXR session, which runs on a Quest and reaches no Android log the app writes; the evidence is the owner's Quest session showing the diagnostic XR view render, respond to input and exit exactly as before the split.
S2760  Adds a measurement schema, two PowerShell commands and two documents, and touches no Kotlin, so there is no changed flow to instrument. What is unobserved is not a device behaviour at all: the first-response and total-completion stages are client-observed by definition, so the baseline runs across the three control classes can only be produced by the owner in the client and handed back through record-run.ps1.
S2757  A retest campaign, not a code change: the remaining work runs the device walk scripts (wear-prerelease-walk.ps1, adb.ps1 clip-check, wear-ink-clip.ps1) on watch hardware, and every .kt flow it re-verifies already carries its own ticket's probe, so there is no S2757 flow entry to instrument. The evidence is the per-screen verdict set in temp/S2757/ from the Phase 02 walk.
S2855  Adds a PowerShell registry CLI, two install-recording hooks and a monitor-page devices section, and touches no Kotlin, so there is no changed flow to instrument. The scripts prove their contracts through their own suites (device-registry.tests, the adb.tests install-record cases, the writer tests devices case); the device pass is one real install through either recorded entry point followed by device-registry.ps1 -Verb Status showing the mark and the monitor page showing the row.
S2883  CI and lint-configuration change: adds a @SuppressLint annotation, a lint disable line and a CI workflow_dispatch job. The only Kotlin touch is the annotation, which changes no runtime flow, so there is no app entry point for a probe. The verification is CI-based: trigger the regenerate-lint-baseline job, then confirm the verify job passes.
S2897  Dialog button focus frame - the change is one android:foreground item added to three button styles in themes.xml, with no Kotlin flow or entry point to instrument. The focus ring is drawn by the platform from the selector drawable when the button gains focus, decided entirely by the resource compiler before any app code runs; the evidence is a visual check on a D-pad/TV - navigate a confirmation dialog and read the 2dp stroke on the focused button (per its own status note).
S2918  MCP agent tooling - config, hooks, commands, scripts and docs only, no .kt touched; the open item is the commit-flow untrack of scripts/mcp/docs-search-mcp/node_modules, verified with git ls-files.
S3010  Release-sweep tree freeze - a PowerShell script, a PreToolUse hook, a gate runner switch, command and rule documents; no .kt touched, so there is no app entry point for a probe. The unobserved part is not on a device at all: the sweep composition (step 0.1 taking the freeze, the step-4 convergence branch re-running only the moved input groups) runs for the first time at the next package pre-release sweep. Unit level is already proven - 12 + 9 + 13 contract cases, and one live refusal through the real hook dispatch on 2026-09-12.
S3065  App-wide wrap-content action buttons - layout XML only (22 files), no Kotlin flow touched, so there is no entry point for a probe. Verification is visual: open each screen of strategic section 5.1 in portrait and landscape and confirm buttons hug their labels, and the Non-goals grids look unchanged.
S3075  CI static-gates runner environment - repository scripts, one workflow file and two documents; nothing ships in an APK, so no .kt exists to carry a probe. The unobserved half is not on a device at all: the forwarder's Linux path, the canon clone and the NOT RUN block only happen on a GitHub runner, and the evidence is the next Android CI run's "Static Gates" job carrying no null-path line and no FAIL over a missing PLAN/ or .claude/. The push that starts it is the owner's action.
S3074  CI verify-job split - one file changed, .github/workflows/android-ci.yml, and it ships in no APK, so no .kt exists to carry a probe. The pending check is not on a device at all: the job layout only runs on a GitHub runner, so the evidence is the next Android CI run finishing without a timeout cancellation and publishing lint-results.xml, test-results xml and the apk-standard-debug artifact. The push that starts it is the owner's action.
S3081  CI action pins off Node 20 - three workflow files only, nothing ships in an APK, so no .kt exists to carry a probe. The pending check cannot run locally at all: the runtimes are resolved by GitHub's runner, so the evidence is the next Android CI run carrying zero Node 20 deprecation annotations and finishing green, with both test-reporter steps publishing into the Job Summary. The push that starts it is the owner's action.
S3085  AppFunctions dependency scope and generated OSS notices only - no Kotlin flow changed, so no app entry point can carry a probe. The relevant evidence is successful FOSS and Legacy manifest/resource processing at minSdk 23 plus a standard debug build retaining the compatible dependency graph.
S3139  Device-profile CSV and settings-reference documentation only, with no Kotlin flow changed. The device check is applying the car-head-unit and photo-frame profiles, then confirming the existing launcher timeout row reads Never and the desktop does not black out by itself.
S3141  Codex bounded-read protocol - repository scripts, one config file and two agent docs only; nothing ships in an APK, so no .kt exists to carry a probe. The pending evidence is not on a device at all: strategic criteria 7 and 8 need an after Codex session transcript measured under the finished protocol, which only exists once a real Codex-authored ticket runs after this one - reproduce with pwsh -NoProfile -File scripts/quality/measure-codex-transcript.ps1 -Id <that ticket> and confirm zero oversized/truncated/sleep-poll/cross-ticket findings.
S2993  Measurement campaign on the phone-watch pair: the ticket changed no Kotlin at all - only PLAN/ result files and two `notes` strings in scripts/devtest/bridge-scenarios.json - so no app entry point exists to carry a probe. What is left is not a code path but two scenarios only the owner can judge: J5 (tiles and complications on four watch surfaces, whose acceptance is a human reading of the picture) and K2 (the phone physically leaving range mid-transfer). Both are driven by hand on the pair, and the verdicts go into PLAN/S2993_phone-watch-pair-test-carryover/results/JK__carryover.md.
S3058  Retest campaign, not a code change: the ticket writes only PLAN/ result files, and every .kt flow it re-verifies belongs to another ticket that already carried its own probe (S3038, S3049, S3051, S3054), so no S3058 flow exists to instrument. What is left is not a code path either - loudness and clap delay by ear, StreamsPlayer open-to-sound and open-to-picture, Wi-Fi off/on on the PC, the barcode on a second phone, the fifth-listener message and an old build against a video descriptor - all of which need the owner, a second phone or the consumer application. Machine evidence of run 2: PLAN/S3058_live-broadcast-end-to-end-retest/results/2026-09-16_run2.md
S3193  RU/UK string values and docs only - the broadcast term renamed in strings.xml of app_v2 and wear plus mirrored docs; no Kotlin flow changed, so no entry point can carry a probe. The device check is reading the renamed labels in Russian on the watch tile and app and on the phone broadcast screen and its notification.
S3194  RU/UK string values and docs only - stream/flow wording renamed to Трансляция and the broadcast title to Вещание in strings.xml and strings_tiles.xml of app_v2 and wear plus mirrored docs; no Kotlin flow changed, so no entry point can carry a probe. The device check is reading the renamed labels in Russian: the watch stream tile, its picker and empty state, pin/unpin in the watch player, the phone statistics screen, the Тестовые трансляции catalog category and the Название вещания row in broadcast settings.
S3195  ES/PT/AR string values only - broadcast terminology renamed to Difusión (ES), Difusão (PT), and إذاعة (AR) in strings.xml and strings_tiles.xml of app_v2 and wear to eliminate collision with streams (Emisiones/Transmissões/البث); no Kotlin flow changed, so no entry point can carry a probe. The device check is reading the distinct labels on the watch and phone in Spanish, Portuguese, and Arabic.
S3168  Layout XML only - equal weights on the three broadcast share buttons in portrait and landscape; no Kotlin flow changed, so no entry point can carry a probe. The device check is opening the broadcast screen on the Russian locale in both orientations and confirming the three buttons take equal thirds with no squeezed last button.
S3236  Layout and dimens XML only - the three broadcast share buttons in portrait and landscape gained uniform text auto-sizing, hyphenation off and tighter horizontal padding; no Kotlin flow changed, so no entry point can carry a probe. The device check is opening the broadcast control screen on the Russian locale in portrait and reading the three captions whole, with no word split across the two lines.
S3294  AR/FR/HI/ZH-Hans string values only - physical_flashlight_title translated in the four locales that had kept the English source text; no Kotlin flow changed, so no entry point can carry a probe. Same class as S3195. The device check is opening the main-screen overflow menu in each of the four languages and reading the flashlight row in that language.
S3296  String resource deletion only - broadcast_entry_live and broadcast_entry_description removed from all thirteen app_v2 locales after S3060 merged away the separate broadcast entry screen that declared them; no Kotlin flow changed, so no entry point can carry a probe. Same class as S1666 (the change is a deletion, so there is no new flow to instrument). The device check is opening the broadcast control screen, starting a broadcast and confirming every caption still renders - the two removed keys were on no surface, so nothing on screen may change.
S3302  EN/ZH/BN/DE/FR/HI/IT/UR string values only - the six broadcast values that still called this device's own outgoing broadcast a stream were renamed to each locale's broadcast word, plus the EN settings annotation for the rowStreamTitle row; no Kotlin flow changed, so no entry point can carry a probe. Same class as S3195 and S3294. The device check is reading the renamed labels in English: the Broadcast title row in broadcast settings, the ongoing notification while broadcasting audio and video, and both permission refusal messages.

S3305  Locale resource values (app_v2/src/main/res/values-*) plus repo scripts; no .kt touched. The evidence is a localized screen on a device set to ar, ur, hi, bn, zh-Hans, ru, uk, de, fr, es, pt or it - a Timber probe cannot observe a string resource rendering.

S3311  Locale resource values (app_v2/src/main/res/values-* and src/vr/res/values-ur) plus one repo script; no .kt touched. Same class as S3305 - the evidence is a localized screen on a device set to ar, bn, hi, ur, zh-Hans, ru or uk, and a Timber probe cannot observe a string resource rendering.
