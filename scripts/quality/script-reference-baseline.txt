# Known unreferenced scripts, judged by assert-script-references.ps1 (S1872, keyed by path S2124).
# One line per script path, so a repaired orphan cannot free a slot the next one occupies silently.
# Before S2124 this file held a single count, and 37 files sharing the name Run-Tests.ps1 shared
# one entry - a list is only possible now that each script is judged by its own path.
#
# A line matching nothing any more is printed as a prune hint, not a failure. A new path not listed
# here fails the gate: delete the script, wire it, or declare it with a
# `Manual tool: <why it exists and who runs it>` line in its comment-based help. Adding a line here
# instead is a decision to keep dead weight, and it should be a deliberate one.
#
# --- Pester runners with no launcher: SECTION CLOSED, S2122 ------------------------------------
# It held 28 lines, one per Run-Tests.ps1 - suites with no place to run from rather than dead code.
# The launcher landed (scripts/quality/run-script-suites.ps1), so they are reached by discovery the
# way a *.Tests.ps1 beside a runner already was, and assert-script-references.ps1 now excuses a
# suite entry point by convention instead. All 28 lines were pruned; do not re-add one. A NEW suite
# needs no line here - only a place on disk under <name>.tests/ or <name>/tests/.

# --- ten lines pruned, S2636, 2026-09-06 -------------------------------------------------------
# A prune hint is not a failure, so the hints accumulated unread until a batch run made them noise
# around a real finding. Nine were the ordinary kind - the script had been wired again, or
# scripts/streams.tests/ gained the Run-Tests.ps1 that puts its four suites back under discovery.
# The tenth was not: capture-play-screenshots.ps1 had never been dead. It was condemned because
# play/ and store_assets/ were missing from the gate's live corpus, so the runbook that invokes it
# was unreadable to the check that judged it. Adding those two roots freed it, and freeing it is
# what proved a baseline line can be wrong rather than merely stale - a line here says "deliberate
# dead weight", and that claim is only as good as the corpus behind it.

# --- scripts nothing references ----------------------------------------------------------------
# Measured 2026-08-21 by S1872 and unchanged by the S2124 re-keying: finished migrations, one-off
# measurements, and wrappers whose headers claim callers they do not have. Most are mentioned only
# in an archived spec or a changelog row, which remembers a script rather than calling one.
scripts/all_features/migrate_from_log.ps1
scripts/check_docs_freshness.ps1
scripts/disable-watchdog-find.ps1
scripts/quality/measure-file-touch-frequency.ps1
scripts/quality/sector-gate-pilot.ps1
scripts/test-compatibility.ps1
scripts/utils/archive-vscode-cruft.ps1
scripts/utils/create-release-candidate.ps1
scripts/utils/generate-quality-report.ps1
scripts/utils/measure-concurrent-build.ps1
scripts/utils/monitor_git.ps1
scripts/utils/setup_test_vr.ps1
scripts/utils/setup-alias.ps1
