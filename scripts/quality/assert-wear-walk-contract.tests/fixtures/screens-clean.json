{
  "_comment": [
    "Fixture: every check satisfied.",
    "S3358: it carries a flavor scope and both catalog bindings, so the clean case pins the accepting",
    "side of those checks too - a gate that only ever fires proves as little as one that never does."
  ],
  "screens": [
    {
      "id": "alpha",
      "name": "Alpha",
      "expect": "Hello",
      "screen": "AlphaScreen",
      "expectRes": "good_marker",
      "homeSection": "APPS",
      "wearApp": "CALCULATOR",
      "flavors": ["noLegal"]
    }
  ],
  "excluded": [
    {
      "screen": "BetaScreen",
      "reason": "not-a-destination",
      "note": "Fixture: nested helper."
    }
  ]
}