# S1639 - Gson persistence contract: exemption registry.
#
# One entry per line: <fully qualified name> :: <justification>
# The name is either a model or enum the gate judges, or a repo-relative source path when the finding is
# a serialization point whose type the gate cannot resolve statically.
#
# An entry with no justification is refused with exit 2. A registry that accepts a bare name degenerates
# into a list nobody rereads, which is the failure strategic S1639 section 7 names as the main threat to
# this gate's value.
#
# The file is a ratchet, like the other count baselines in this repository: removing an entry is always
# accepted, adding one is a deliberate act that shows up in review.
#
# A justification opening with `Ticket: Sxxxx` records a live defect owned by that ticket rather than
# excusing it. Those are counted separately in the verdict line, so a green run states out loud how many
# known defects it is still carrying.

# --- Live defects, owned and tracked -----------------------------------------------------------------
# (none)

# --- Points whose type the gate cannot resolve, with what they actually write -------------------------
app_v2/src/main/java/com/sza/fastmediasorter/data/repository/CachedFileListRepository.kt :: the read passes a named TypeToken field declared elsewhere in the file; the writes in the same file resolve to MediaFile, which is judged and kept by rule.
app_v2/src/main/java/com/sza/fastmediasorter/data/repository/wear/WearResourceStampStore.kt :: the three unresolved points are the S2502 resource edit-stamp map, a Map<String, Long> read and written through a TypeToken; the keys are resource ids carried as data and the values are epoch millis, so there is no model behind it, no field name to pin and nothing R8 can rename. Same shape and same reason as the settings field-stamp map on the line below.
app_v2/src/main/java/com/sza/fastmediasorter/data/repository/wear/WearSettingsMirrorStore.kt :: the two unresolved points are the S2093 field-stamp map, a Map<String, Long> whose keys are contract field names carried as data and whose values are epoch millis, so there is no model behind it and nothing R8 can rename; the payload points in the same file resolve to WearSettingsPayload, which is judged and kept by rule.
app_v2/src/main/java/com/sza/fastmediasorter/domain/model/WearEventEnvelopeCodec.kt :: writes a hand-built com.google.gson.JsonObject, not a model, so the gate has no declared type to read and there are no field names to pin; the four wire names live as constants in the codec's own companion and are fixed on both sides by the cross-module WearEventEnvelopeCodecTest fixture (S1893).
app_v2/src/main/java/com/sza/fastmediasorter/domain/usecase/BackupToGoogleDriveUseCase.kt :: the payload is the result of a use case invocation, so its type is declared in another file; BackupPayload is judged through its other points and kept by rule.
app_v2/src/main/java/com/sza/fastmediasorter/domain/usecase/ExportSettingsUseCase.kt :: same use case invocation as the Drive backup path, therefore the same BackupPayload.
app_v2/src/main/java/com/sza/fastmediasorter/domain/usecase/RestoreDeletedUseCase.kt :: reads TrashMetadata through the model's own fromJson() factory; TrashMetadata is judged and kept by rule.
wear/src/main/java/com/sza/fastmediasorter/wear/data/repository/WearFavoritesRepositoryImpl.kt :: writes the delta list, given once as a call type argument and once through a local chain; WearFavoriteDeltaItem is kept by the wear package rule.
wear/src/main/java/com/sza/fastmediasorter/wear/data/wear/WatchWearListenerService.kt :: reads a bare String and rebuilds the command with valueOf, so the value at risk is the constant name of the watch's own WearPlaybackCommand, kept by the wear package rule.
wear/src/main/java/com/sza/fastmediasorter/wear/domain/model/WearEventEnvelopeCodec.kt :: writes a hand-built com.google.gson.JsonObject, not a model, so the gate has no declared type to read and there are no field names to pin; the four wire names live as constants in the codec's own companion and are fixed on both sides by the cross-module WearEventEnvelopeCodecTest fixture (S1893).
wear/src/main/java/com/sza/fastmediasorter/wear/data/repository/WearPhonePinsRepository.kt :: persists a plain Array<String> of folded channel identities - there is no model, so there are no field names to pin and minification has nothing to rename; the gate cannot resolve the type because the identifier is a private file-local val. The values themselves are addresses produced by foldWearStreamIdentity, whose spelling is fixed by WearStreamIdentityFoldTest (S2149).
app_v2/src/main/java/com/sza/fastmediasorter/data/repository/wear/WearResourceIdAliasStore.kt :: persists a plain Map<String, Long> from a watch-created source id to the phone resource id issued for it - there is no model, so there are no field names to pin and minification has nothing to rename. Both halves of every entry are values rather than identifiers: the key is a UUID the watch generated and the value is a Room row id, and the read side is pinned by the declared TypeToken in the store's own companion (S2507).
