# Pointer - `REPO-LAYOUT`

| | |
| --- | --- |
| **Id** | `REPO-LAYOUT` |
| **Version** | 0.9, draft. Owner: the canon (sza-unified-rules) |
| **Home** | `rule-adoption/README.md` section 4 in the shared contracts catalog |
| **Role here** | adopter - keeps the names tools address by |

## What this repository must do to stay conformant

- The agent-rules file, `README.md` and `LICENSE` stay at the repository root.
- One pointer per contract in this folder, named after its id, indexed by `README.md` here.
- The ledger stays where the stamp says it is (`ledgerShape` in `.sza-canon.json`).
- An archive tree is frozen and never "fixed".

## Where it lives here

- `CLAUDE.md`, `AGENTS.md`, `README.md`, `LICENSE` at the root.
- `docs/contracts/` - this folder.
