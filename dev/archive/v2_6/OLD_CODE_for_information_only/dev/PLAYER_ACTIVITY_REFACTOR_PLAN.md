# Refactoring Plan: PlayerActivity.kt Decomposition - PENDING

## Goal
Further decouple `PlayerActivity.kt` by moving remaining media-specific logic into `PdfViewerManager`, `TextViewerManager`, and `EpubViewerManager`.

**STATUS**: ⏸️ **ROLLED BACK / NOT STARTED**

---
