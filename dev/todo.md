Я хочу иметь немного другую иконку для дебаг версии (с маленькой буквой D, другую для лайт версии и других. Только у основной версии программы чтобы была текущая иконка.

Я хочу чтобы у экранов (страниц) активити Welcome были разный цвета бэкграунда, не яркие.

При прохождении велком экрана я не получил вопросв про права на медаиа.
Важно чтобы программа преупреждала, что таких прав нет. Перед созданием нового локального ресурса, если их нет.
В настройках, если дать права, кнопки запроса прав "неактивные". Желательно (если права есть) то переименовать их в "управление правами" и по ним чтобы открывался тот же диалог ОС, в котором право можно отобрать.

В интерфейсе страницы настроек-основные вынести всё что касается DEBUG в отдельную группу (которую не станет видно в релизной версии)

Если ресурс только музыкальный (выбрано только Аудил) то у него специальное поведение в BROWSE - все миниатюры 48 на 48б не важно что там в настройках и у него нет режима сетки и на него нельзя переключиться (только список).

У нас есть файлы с генерируемыми миниатюрами (TXT, MP3..) по расширению. Но у всех них одинаковый фон. Я бы хотел, чтобы программа генерировала немного различные фоны для миниатюр для различных типов файлов. Ну например чтобы все музыкальные - были желтые, А все mp3- чуть оранжевыми. А, скажем flac и ape - светло желтые. Подобное для документов и бинарных файлов - группы цветов с лёгкими градациями под конкретные популярные разрешения.

Когда у картинки, видео, PDF, EPUB нет миниатюры - (они ещё загружаются / не смогли загрузится по любой причине), я хочу чтобы файл получал такую же сгенерированную миниатюру по разрешению файла, по тем же законам как для файлов, у которых миниатюр нет. Пока его миниатюра не загрузится.

Если я проигрываю файл по сети (например на FTP) и выбираю его для отправки "Android share" то прорамма его копирует локально перед отправкой. Но об этом нет прогресса пользователю - для него будто программа зависла.

При проигрывании изображения в полном экране не работают 9 тачзон.

Во время листания в режиме с вомандной панелю, если я перехожу на изображение, но после документа (PDF) то не работают тач зоны предыдущий/следующий. При этом центральная область (зум) работает. Возможно она одинаковая для документа.

В Browse когда нажимаю кнопкау "выбрать все" то хочу получить тост-сообщение "выбрано NN файлов"

Ошибся при вводе пароля и в итоге очень долго ждал SMB ошибку. Странно ведь я получил ответ - неверный пароль. Зачем сработал тафмаут?

После множественных ошибок пишет "smb connction reset" но это не работает - пока не закроешь программу совсем и не откоешь заново - связь по SMB Не восстанавливатся ни с одним ресурсом.