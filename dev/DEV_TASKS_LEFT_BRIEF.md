# Оставшиеся задачи разработки

## 1. UI Features (UI_FEATURES_SPEC.md)

**Статус:** 🟡 Планируется (Не реализовано)
**Приоритет:** Средний

Список задач для улучшения пользовательского интерфейса:

- **Массовое переименование файлов** (8-12ч): Позволяет переименовывать несколько файлов по шаблону.
- **Подсказки жестов** (4-6ч): Оверлей с подсказками для новых пользователей.
- **Ручная синхронизация облака** (6-8ч): Кнопка "Синхронизировать сейчас" в настройках.
- **Миграция на SurfaceRenderer** (15-20ч): Завершение перехода с PhotoView на SurfaceView для улучшения производительности.
- **Редактирование PDF** (20-30ч): Поворот, удаление страниц (низкий приоритет).
- **Обрезка изображений при просмотре (Crop to full screen)**: Требует проверки поведения при повороте экрана.

---

## 2. Статус реализации (MASTER_IMPROVEMENT_PLAN.md)

**Статус:** ✅ Заявлено как 100% готово.

Все 54 шага отмечены как выполненные.

- **Track D (Static Image):** Рендерер включен по умолчанию (`rendererMigrationEnabled = true`).
- **Слайд-шоу:** Функция "не выключать экран" реализована.

## 3. Облачная диагностика

Скрипты диагностики и документация по облакам (`CLOUD_*.md`) остаются актуальными справочными материалами.

## 4. Архив

Все старые спецификации (`SPEC_*.md`, `TODO_SPECS_INDEX.md` и др.) будут перенесены в папку `temp/archive`, так как они либо выполнены, либо дублируются в мастер-плане.
