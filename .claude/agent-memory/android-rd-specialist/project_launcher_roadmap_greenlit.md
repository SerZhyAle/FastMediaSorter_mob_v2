---
name: launcher-roadmap-greenlit-2026-07
description: Launcher family state - S0404/S1088 archived but children live; 2026-07-18 and 2026-07-27 batch quizzes locked their design decisions; owner drives them by explicit ask, never via /spec-next
metadata:
  type: project
---

On 2026-07-18 the owner greenlit the whole launcher roadmap (epic S0404) via a batch `/spec-quiz` (16 decisions, master gate Q0=A). The tactical freeze that previously parked its child feature tickets as `BlockQuestions` no longer applies.

**Why:** during the earlier `/spec-next` launcher run these tickets were blocked precisely because S0404 was owner-gated and each child needed an explicit greenlight + design call. The owner has now given all of them in one sheet (`temp/scratch/launcher-quiz-questions.md`).

**How to apply:**
- Treat launcher feature tickets (S1087, S1091, S1093, S1094, S1103, S1107, ..) as drivable Drafts - do NOT re-block them as "needs greenlight / owner-gated". They were restored BlockQuestions -> Draft.
- Build order: **S1088** (System launcher settings dialog) is the FOUNDATION - author + build it first. S1090 (lock desktop) and S1101 (wallpaper) stay `BlockByOtherTask(S1088)` until it lands; S1102 (launcher user docs) stays blocked until the feature surface lands.
- **S1098** (clickable status indicators) was ARCHIVED - owner deprioritized the status-area replace path (S1087 stays default-OFF, `S1098-1: N/A`). Revive only if that path is later prioritized.
- Non-obvious design decisions are locked in each spec's `### Quiz decisions (2026-07-18)` block. Deviations from the recommended default worth remembering: S1093 resize = drag-handles + any-widget-can-go-fullscreen (no per-widget cap); S1103 "scheduled operations" cell triggers the op directly (not open the schedules screen); S1107 onboarding-launcher fix = variant B (deferred HOME-role request from Settings/Main, amends ADR-2's "chooser on next Home press").
- S1107 is pri 90 (bug) and fully diagnosed - fast-track candidate ahead of the pri-50 feature tickets.

**UPDATE 2026-07-22 (owner explicitly drove 8 launcher-polish tickets):** the owner batch-requested `implement S1094, S1099, S1103, S1089, S1091, S1092, S1093, S1095` and I drove all 8 through the full pipeline (spec-tech -> spec-dev -> phase audits). All 8 landed in **BlockNeedUserTest** on 2026-07-22, awaiting a batched `/spec-sweep` on device (emulator-5554 was online but device tests were deferred to one sweep). Explicit owner request OVERRIDES the "do not autonomously drive launcher-family" note below - that note is about `/spec-next`, not an owner ask. Key outcomes worth remembering:
- **S1099 needed no code** - "don't cover the nav bar" was already implemented by `applySystemBarInsetPadding()` reserving the bottom inset on `launcherRoot` + no immersive hide. Filled the empty Draft, set BNUT for device confirm only.
- **S1093 (resize)** added `LauncherDesktopRepository.resizeCell` (mirrors `moveCell`, reuses `findOverlapping` excludeId) + `LauncherResizeManager` (preview-overlay gesture: real cell never mutated during drag, only the cells Flow commits) + a bottom-right handle on GADGET cells. Floor = gadget seed, ceiling = full width x viewport rows.
- **S1094** decoupled `LauncherGadget.minSpanW/minSpanH` (resize floor, default getters = defaultSpan) from `defaultSpanW/H` (seed) - clock seeds 4x2 but floors at 2x1; S1093's `seedFloor` now reads `minSpan*`. Long-press clock -> calendar (ACTION_VIEW CalendarContract), tap -> alarms, seconds added.
- **S1103 = Part 1 only** - `LauncherCellCommand.ScheduledOp(op:<id>)` (new sealed case + tolerant codec, no Room migration), a "Quick-access panel" `InternalRouteCatalog` route (self-referential panel-editor tile accepted, no per-consumer filter), and a scheduled-op cell that tap -> confirm dialog -> background run + toast (owner 2026-07-22, because ops can DELETE/MOVE). **Part 2 (third-party app-shortcuts) stays blocked on S0427** - strategic criterion 3 is open; `/spec-check` will likely mark S1103 `Partial`.
- Shared-infra additions reused cleanly: `SearchableOptionPickerController.attach` gained an optional `columns: Int = 1` (S1095 app picker grid; 7 other pickers untouched via the default).

**UPDATE 2026-07-24 (where the launcher desktop physically lives):** the desktop UI ships from its own source set `app_v2/src/launcherEnabled/` - `ui/launcher/grid/LauncherCellViewBinder.kt`, `ui/launcher/gadget/{LauncherGadget,LauncherGadgetRegistry,FolderPreviewGadget,PlaylistGadget,StreamsGadget}.kt`, plus the `category.HOME` entry in `src/launcherEnabled/AndroidManifest.xml`. Domain/data stay in `src/main` (`LauncherCell` with kinds `SHORTCUT`/`GADGET` and per-orientation spans, `LauncherCellDao`, `LauncherDesktopRepositoryImpl`, `ResolveLauncherDesktopUseCase`). Any new desktop capability is a registration in `LauncherGadgetRegistry` placed in that source set - never a `BuildConfig` guard in `src/main`. Note there is **no `AppWidgetHost` anywhere in the project**. ~~so hosting real Android app widgets on the launcher desktop is a new subsystem (that is S1170)~~ - **STALE, corrected 2026-07-27:** S1170 is NOT about hosting foreign widgets. The 2026-07-24 quiz decided it means placing *our own* 14 catalog widgets on the desktop as hand-written gadgets, so `AppWidgetHost`, `bindAppWidgetIdIfAllowed` and the `BIND_APPWIDGET` dialog are all out of scope. S1170 is `Tactical` since 2026-07-27 with a 7-phase plan.

**UPDATE 2026-07-24 (orphan-child call made for S0427):** the owner chose UNBLOCK, not archive, for the first of the three orphaned research-children: **S0427** (third-party app-shortcuts) went `BlockByOtherTask` -> Approved -> Tactical -> `BlockNeedUserTest` in one session, with the UI question answered "идиома стокового лаунчера" (long-press popup, cap 5). Implementation: `AppShortcutDataSource` (`src/main/data/launcher/`, the only `LauncherApps` seam) + `QueryAppShortcutsUseCase`/`StartAppShortcutUseCase`, popup in `src/launcherEnabled/ui/launcher/helpers/LauncherAppShortcut{Adapter,MenuManager}.kt`, long-press wired on desktop `app:` cells and the Start-menu grid. Two consequences worth carrying: (1) the launcher surface ships only in `standard`+`noLegal` (both minSdk 26), so the API-25 floor and any `SDK_INT` guard for shortcut APIs are moot - specs still repeating the "API < 25 degradation" line are describing a case that cannot occur; (2) S1103's deferred Part 2 is now covered, so its `Partial` verdict should be re-checked. **S0426** and **S0429** remain orphaned with stale blocker notes - same owner call still open.

**UPDATE 2026-07-27 (second batch quiz - the launcher idea-inbox is now decided, and the orphan set shrank):** the owner ran `/spec-quiz` over six blocked tickets in one session (11 decisions). Outcome: **S1188 + S1170 -> Approved**, **S1175 + S1176 + S1178 + S1179 -> Draft** (bodies still skeletons - `/spec` must author them from their `### Quiz decisions (2026-07-27)` blocks). Two enablers parked: **S1205** (accept `ACTION_CONFIRM_PIN_SHORTCUT` from third-party apps so the desktop can host e.g. a Maps "where is this person" shortcut - the intent of a foreign pinned shortcut is unreadable, but the default launcher can launch it by id) and **S1206** (READ_CONTACTS live contact data, deliberately out of S1176's v1). Owner pattern worth carrying: he consistently picks the **wider** scope over the staged one (all 14 widgets, all 4 gadgets, all 4 Maps items) - do not assume the "9 simple now, ticket the rest" style recommendation will win. Also: S0426 is now `BlockNeedUserTest` and its `WeatherGadget` ships in `src/launcherEnabled/` (the orphan note below is stale for it), S0429 is `Approved`, S0427 and S1103 are `BlockNeedUserTest`.

**UPDATE 2026-07-27 (planning pass + two follow-up quizzes):** S1170 and S1176 are now `Tactical` with authored plans; S1178 is `Approved`; S1175 is `BlockByOtherTask(S1205)` and **S1205 is `Approved` and now the critical path** - it blocks S1175 entirely and covers S1103's deferred Part 2. Decisions locked, including two where the owner overrode my recommendation:

- **S1175:** place entry is **Share-from-Google-Maps only** - no place picker, no address field, no interactive map. The owner also chose to hold *all four* goals until S1205 rather than shipping goals 1-3 first, even though only goal 4 technically needs it. Address-with-suggestions is closed **permanently**, not deferred (Nominatim forbids client-side autocomplete; every provider offering it needs a paid key) - see [[no-paid-or-key-third-party-services]]; do not re-offer it as a scope option.
- **S1205:** label/icon come from the pin request only (no rename); a dead shortcut is **marked inactive, not auto-removed**; and the pin request is **accepted silently with a toast** - the owner rejected my confirm-dialog recommendation. Implementation trap worth carrying: "silently" still needs the `ACTION_CONFIRM_PIN_SHORTCUT` activity - it just finishes without drawing a dialog.
- Two facts found while planning, easy to re-discover the hard way: `LauncherCell.labelOverride` exists end-to-end (model, entity, `ResolveLauncherDesktopUseCase`) but **every desktop write site passes null** - there is no rename UI for any cell kind; and the launcher has **no package-removal handling at all**, so today a dead `app:` cell looks alive and its tap silently fails.

**UPDATE 2026-07-20 (supersedes the greenlit framing above):** the live catalog now shows **S0404 (epic) and S1088 (foundation dialog) = `Archived`** (updated 2026-07-18 19:23). The launcher-mode-profiles direction was shelved, NOT driven. Yet the child feature tickets (S1087, S1089-S1095, S1099, S1101-S1103) plus bugfixes S1096/S1097 remain **live** (Draft/BlockNeedUserTest) and skip-cached as "concurrent-wip: active uncommitted system-launcher implementation". Net: do NOT autonomously drive launcher-family tickets - their epic is archived, so `/spec-next` correctly leaves them skip-cached; the "tree settled -> re-check concurrent-wip" note in [[debug-v026-tree-settled-2026-07-19]] does NOT mean these are drivable. Orphaned-children hygiene (archive the kids too, or un-archive the epic) is an owner call - flag it, don't act. The orphan set also includes the two research-children extracted from S0404 research - **S0426** (home-screen weather info-block) and **S0429** (Google content on the home surface) - both still `BlockByOtherTask` with a status note naming S0404 as blocker; that note is stale twice over (epic archived, and the desktop/GADGET contract they waited on now exists in code).
