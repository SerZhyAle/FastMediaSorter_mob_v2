---
description: "Alias -> /spec-draft. Generated; edit the table in scripts/utils/generate-command-aliases.ps1."
argument-hint: "same arguments as /spec-draft"
disable-model-invocation: true
metadata:
  alias_of: spec-draft
  alias_set: shadow
  generated_by: scripts/utils/generate-command-aliases.ps1
---

`/ыв` is a short alias for `/spec-draft` and carries no behaviour of its own.

Invoke the Skill tool with skill `spec-draft`, passing `$ARGUMENTS` as its args, and follow
that command exactly. Do nothing before that call and do not restate this file - the operator
typed a shortcut, not a new instruction.