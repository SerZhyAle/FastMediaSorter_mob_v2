---
description: "Alias -> /spec-arc. Generated; edit the table in scripts/utils/generate-command-aliases.ps1."
argument-hint: "same arguments as /spec-arc"
disable-model-invocation: true
metadata:
  alias_of: spec-arc
  alias_set: translit
  generated_by: scripts/utils/generate-command-aliases.ps1
---

`/арк` is a short alias for `/spec-arc` and carries no behaviour of its own.

Invoke the Skill tool with skill `spec-arc`, passing `$ARGUMENTS` as its args, and follow
that command exactly. Do nothing before that call and do not restate this file - the operator
typed a shortcut, not a new instruction.