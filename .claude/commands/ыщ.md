---
description: "Alias -> /spec-do. Generated; edit the table in scripts/utils/generate-command-aliases.ps1."
argument-hint: "same arguments as /spec-do"
disable-model-invocation: true
metadata:
  alias_of: spec-do
  alias_set: shadow
  generated_by: scripts/utils/generate-command-aliases.ps1
---

`/ыщ` is a short alias for `/spec-do` and carries no behaviour of its own.

Invoke the Skill tool with skill `spec-do`, passing `$ARGUMENTS` as its args, and follow
that command exactly. Do nothing before that call and do not restate this file - the operator
typed a shortcut, not a new instruction.