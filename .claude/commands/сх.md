---
description: "Alias -> /spec. Generated; edit the table in scripts/utils/generate-command-aliases.ps1."
argument-hint: "same arguments as /spec"
disable-model-invocation: true
metadata:
  alias_of: spec
  alias_set: translit
  generated_by: scripts/utils/generate-command-aliases.ps1
---

`/сх` is a short alias for `/spec` and carries no behaviour of its own.

Invoke the Skill tool with skill `spec`, passing `$ARGUMENTS` as its args, and follow
that command exactly. Do nothing before that call and do not restate this file - the operator
typed a shortcut, not a new instruction.