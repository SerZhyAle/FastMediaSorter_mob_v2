---
description: "Alias -> /spec-quiz. Generated; edit the table in scripts/utils/generate-command-aliases.ps1."
argument-hint: "same arguments as /spec-quiz"
disable-model-invocation: true
metadata:
  alias_of: spec-quiz
  alias_set: shadow
  generated_by: scripts/utils/generate-command-aliases.ps1
---

`/ый` is a short alias for `/spec-quiz` and carries no behaviour of its own.

Invoke the Skill tool with skill `spec-quiz`, passing `$ARGUMENTS` as its args, and follow
that command exactly. Do nothing before that call and do not restate this file - the operator
typed a shortcut, not a new instruction.