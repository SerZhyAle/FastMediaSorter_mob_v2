---
description: "Alias -> /spec-dev. Generated; edit the table in scripts/utils/generate-command-aliases.ps1."
argument-hint: "same arguments as /spec-dev"
disable-model-invocation: true
metadata:
  alias_of: spec-dev
  alias_set: translit
  generated_by: scripts/utils/generate-command-aliases.ps1
---

`/сш` is a short alias for `/spec-dev` and carries no behaviour of its own.

Invoke the Skill tool with skill `spec-dev`, passing `$ARGUMENTS` as its args, and follow
that command exactly. Do nothing before that call and do not restate this file - the operator
typed a shortcut, not a new instruction.