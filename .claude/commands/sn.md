---
description: "Alias -> /spec-next. Generated; edit the table in scripts/utils/generate-command-aliases.ps1."
argument-hint: "same arguments as /spec-next"
disable-model-invocation: true
metadata:
  alias_of: spec-next
  alias_set: latin
  generated_by: scripts/utils/generate-command-aliases.ps1
---

`/sn` is a short alias for `/spec-next` and carries no behaviour of its own.

Invoke the Skill tool with skill `spec-next`, passing `$ARGUMENTS` as its args, and follow
that command exactly. Do nothing before that call and do not restate this file - the operator
typed a shortcut, not a new instruction.