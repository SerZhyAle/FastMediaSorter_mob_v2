---
description: "Alias -> /spec-fix. Generated; edit the table in scripts/utils/generate-command-aliases.ps1."
argument-hint: "same arguments as /spec-fix"
disable-model-invocation: true
metadata:
  alias_of: spec-fix
  alias_set: latin
  generated_by: scripts/utils/generate-command-aliases.ps1
---

`/sf` is a short alias for `/spec-fix` and carries no behaviour of its own.

Invoke the Skill tool with skill `spec-fix`, passing `$ARGUMENTS` as its args, and follow
that command exactly. Do nothing before that call and do not restate this file - the operator
typed a shortcut, not a new instruction.