# Phase 08 - Docs / Catalog Cleanup

**Strategic spec:** [`../S0290_vr_test_quality_overhaul.md`](../S0290_vr_test_quality_overhaul.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** ⬜ Not started
**Depends on:** all previous phases
**Blocks:** none — final phase
**Steps done:** 0 / 3
**Started:** -
**Completed:** -

---

## Objective

Final consolidation: regenerate catalog indexes for new public API (`VrMaterialFormatDetector` and strategies), ensure `THIRD_PARTY_LICENSES.md` accurately reflects all bundled / pushed assets after Phase 05 expansion, and confirm dev changelog has an entry per modified source file.

---

## Prerequisites

- [ ] Phases 01-07 ✅ Done.
- [ ] Working tree is clean or on a feature branch.

---

## Files Touched

| File | New / Modified | Line budget |
|------|:--------------:|------------:|
| `dev/CATALOG/app_v2.jsonl` | Regenerated by script | n/a (script-managed) |
| `dev/CATALOG/app_v2.md` | Regenerated by script | n/a (script-managed) |
| `THIRD_PARTY_LICENSES.md` | Modified | ≤ 200 (current 39 + new asset entries from Phase 05) |
| `dev/CHANGELOG.md` | Appended via `add_to_dev_log.ps1` calls | n/a (append-only) |

---

## Steps

### Step 08.1 - Regenerate catalog for app_v2

**Files:** `dev/CATALOG/app_v2.jsonl`, `dev/CATALOG/app_v2.md`
**Depends on:** start of phase

**Prompt for developer:**

> Run `pwsh -NoProfile -File scripts/catalog_sync.ps1 -Module app_v2` to regenerate the catalog indexes. After regeneration, run `pwsh -NoProfile -File dev/CATALOG/scripts/set.ps1 -ClassMatches "*VrMaterialFormatDetector" -NoFlavors "standard,lite,photos,legacy"` and similar for the strategy classes (`VrFilenameFormatDetector`, `Mp4BoxStereoStrategy`, `XmpGPanoStrategy`, `AspectRatioStrategy`, `SadSymmetryStrategy`, `VrCompositeFormatDetector`) to mark them as vr/noLegal-only in the catalog metadata. Wrap in try/catch per memory note `project_catalog_set_ps1_stops_on_error` to avoid one missing entry aborting the batch.

**Verification:**

- `Grep` - `VrMaterialFormatDetector` matches at least once in `dev/CATALOG/app_v2.jsonl`.
- `Grep` - `VrCompositeFormatDetector` matches at least once in `dev/CATALOG/app_v2.jsonl`.
- `Grep` - `"role"` field present for new classes in the catalog.

**Status:** `[ ]` not done

---

### Step 08.2 - Update THIRD_PARTY_LICENSES.md with Phase 05 assets

**Files:** `THIRD_PARTY_LICENSES.md`
**Depends on:** Step 08.1

**Prompt for developer:**

> For each new asset added by `setup_test_vr.ps1` in Phase 05 that is **bundled into the APK or distributed alongside it** (= none in this spec — Phase 05 assets live only on the dev-host's `/sdcard/Pictures/FastMediaSorterVrTest/` after push), no licenses entry is required. Confirm the existing Poly Haven CC0 entry for `vr_diagnostic_360_mono.jpg` is still accurate. If any Phase 05 sample is later promoted to APK-bundled, add a section per the existing template.
>
> Practical action for this phase: re-read the file, confirm no stale "Navier8 / blender_test.jpg" reference survived, leave content unchanged if all is well. Document in chat: "no asset licensing changes required — all Phase 05 samples are dev-host-only".

**Verification:**

- `Grep` - `Navier8` matches zero times in `THIRD_PARTY_LICENSES.md`.
- `Grep` - `vr_diagnostic_stereo_tb` matches zero times in `THIRD_PARTY_LICENSES.md`.
- `Grep` - `Poly Haven` matches at least once in `THIRD_PARTY_LICENSES.md`.

**Status:** `[ ]` not done

---

### Step 08.3 - Verify dev changelog completeness

**Files:** `dev/CHANGELOG.md`
**Depends on:** Step 08.2

**Prompt for developer:**

> Final pass: confirm every file modified in Phases 01-07 has a corresponding entry in `dev/CHANGELOG.md`. The earlier phases each ran `.\scripts\add_to_dev_log.ps1` at completion — this step is verification only. If gaps are found, append the missing entries now. Do **not** edit `dev/CHANGELOG.md` by hand for content other than missing entries — the file is append-only via the script per CLAUDE.md "Post-Change Steps". Also confirm that neutral development logs introduced earlier are either still justified as permanent diagnostics or are removed before the final user-test handoff; only the deliberate `S0290:` probes inserted during the `BlockNeedUserTest` transition may keep the ticket prefix.

**Verification:**

- `Grep` - `S0290` matches at least once in `dev/CHANGELOG.md` (any phase's entry will hit this).
- `Grep` - `xr_session.cpp` matches at least once in `dev/CHANGELOG.md` (Phase 01 + Phase 06 + Phase 07 touched it).
- `Grep` - `DiagnosticXrActivity.kt` matches at least once in `dev/CHANGELOG.md`.
- `Grep` - `setup_test_vr.ps1` matches at least once in `dev/CHANGELOG.md`.
- `Grep` - `VrMaterialFormatDetector` matches at least once in `dev/CHANGELOG.md`.

**Status:** `[ ]` not done

---

## Phase Done Criteria

- [ ] Every `Step 08.*` above is `[x] done`.
- [ ] No `TODO(phase-08)` in repo.
- [ ] Catalog regenerated; both `app_v2.jsonl` and `app_v2.md` are fresh (per gitignore they are local only — no commit expected).
- [ ] On-device sanity run: full playlist cycle completes without errors; HUD label correctly identifies each material's format; lakeside.jpg loads first; bug fixes from Phase 01 hold; render quality from Phase 06 holds; equirect2/FFR flags work as set per §6.2/§6.3.
- [ ] Strategic spec status flipped to `BlockNeedUserTest` via `pwsh -NoProfile -File scripts/spec_catalog/update.ps1 -Id S0290 -Status BlockNeedUserTest` to await owner verification on Quest 3, and the deliberate ticket-scoped `S0290:` probes required by CLAUDE.md are present only at the chosen flow-entry points for that handoff.

---

## Handoff Notes to Next Phase

Final phase — see INDEX.md Completion Gate. After owner confirms on-device behaviour, `/spec-check S0290` flips status to `Verified` and the `S0290:` Timber probes are removed automatically by that skill.

## Revision History

- **2026-05-22** - by `/spec-update` (`GPT-5.4`, focus: consistency, completeness, verifiability, stability)
	- Applied: 2. Proposed (DISCUSS): 0.

---

## Rollback Plan

Catalog and changelog are reversible by re-running the regen scripts after a code revert. Licensing doc revert is a single-file restore. No data migration involved at any point in this spec.
