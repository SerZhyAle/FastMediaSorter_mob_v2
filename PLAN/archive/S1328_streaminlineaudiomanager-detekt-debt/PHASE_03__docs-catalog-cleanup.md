# Phase 03 - Docs and catalog cleanup

**Strategic spec:** [`../S1328_streaminlineaudiomanager-detekt-debt.md`](../S1328_streaminlineaudiomanager-detekt-debt.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** ✅ Done
**Depends on:** Phase 02
**Blocks:** none - final phase
**Steps done:** 3 / 3
**Started:** 2026-08-03
**Completed:** 2026-08-03

---

## Objective

Register the new holder class in the class catalog, journal the change, and prove the ticket closed
without a suppression or a baseline edit.

---

## Prerequisites

- [ ] Phase 01 and Phase 02 are ✅ Done.
- [ ] Working tree is clean or on a feature branch.

---

## Files Touched

| File | New / Modified | Line budget |
|------|:--------------:|------------:|
| `dev/CATALOG/app_v2.jsonl` | Modified (regenerated, gitignored) | n/a |
| `dev/CHANGELOG.md` | Modified (via script, never by hand) | n/a |

> `dev/CATALOG/app_v2.jsonl` and its `.md` twin are local gitignored indexes - regenerate, never commit.

---

## Steps

### Step 03.1 - Regenerate the class catalog and classify the new file

**Files:** `dev/CATALOG/app_v2.jsonl`
**Depends on:** - start of phase

**Prompt for developer:**

> Run `pwsh -NoProfile -File scripts/catalog_sync.ps1 -Module app_v2` once for the whole ticket, then
> fill `role` and `status` for the new `StreamInlineAudioDependencies` entry with
> `dev/CATALOG/scripts/set.ps1`. The two holders are not flavor-gated, so no `-NoFlavors` hint
> applies. Confirm `StreamInlineAudioManager`'s catalog entry picked up the new four-parameter
> constructor.

**Verification:**

- `Grep` - `StreamInlineAudioDependencies` matches at least once in `dev/CATALOG/app_v2.jsonl`.
- `Grep` - the `StreamInlineAudioDependencies` record has a non-empty `role` and `status`.
- `Grep` - `StreamInlineAudioViews` matches in `dev/CATALOG/app_v2.jsonl`.

**Status:** `[x]` done

**Step Log:**

- 2026-08-03 - Verification 3/3 PASS. The catalog was regenerated by the Phase 02 `post-change.ps1` run; `set.ps1` then filled `role` and `status=new` on both holder records (one path, two records). `StreamInlineAudioManager`'s own record now reads `constructorDeps: [LifecycleOwner, StreamInlineAudioViews, AudioServiceController, StreamInlineAudioCallbacks]`, so the four-parameter shape is what the index carries.

---

### Step 03.2 - Journal the change

**Files:** `dev/CHANGELOG.md` (written only through the script)
**Depends on:** Step 03.1

**Prompt for developer:**

> Add one dev-log entry for the whole ticket, not one per file, via
> `./scripts/add_to_dev_log.ps1` (or batch the four source paths through
> `close-and-log.ps1 -DevLogs`). Do not hand-edit `dev/CHANGELOG.md`. Skip
> `scripts/all_features/add.ps1`: this ticket ships no user-visible capability, only a constructor
> shape. Skip the settings-manifest regen and the strings audit - no setting and no string changed.

**Verification:**

- `Grep` - `dev/CHANGELOG.md` contains an entry naming `StreamInlineAudioManager`.
- `Grep` - `docs/ALL_FEATURES.jsonl` is unchanged by this ticket (no new record mentioning `StreamInlineAudio`).

**Status:** `[x]` done

**Step Log:**

- 2026-08-03 - Verification 2/2 PASS. One dev-log row for the whole logical change, written by `post-change.ps1` at the Phase 02 boundary and naming `StreamInlineAudioManager`; `dev/CHANGELOG.md` was never opened by hand. `docs/ALL_FEATURES.jsonl` carries zero `StreamInlineAudio` mentions - the ticket ships a constructor shape, not a capability.

---

### Step 03.3 - Prove the closure predicates

**Files:** none - verification over already-changed files
**Depends on:** Step 03.2

**Prompt for developer:**

> Run the detekt gate over the full changed set, not through `post-change -ScopeToFile`, which
> narrows to a single `-File` and would report PASS while a sibling file carries findings:
> `pwsh -NoProfile -File scripts/quality/assert-detekt.ps1 -Module app_v2 -Gate -ChangedFiles` with
> all four source paths. Require the literal verdict line `PASS [scoped]` - the script exits 0 in
> non-gate mode even when it found something, so read the verdict rather than `$?`. If the report is
> older than the newest edited file the script exits 2 (cannot verify); refresh it with
> `:app_v2:detekt --rerun-tasks` and re-run rather than accepting the stale result.

**Verification:**

- Value equality - `assert-detekt.ps1 -Gate` over the four files prints its PASS verdict and exits 0. The literal string this predicate was written against, `PASS [scoped]`, is not what the script emits: the current wording is `assert-detekt: PASS [app_v2] (no new findings; baselines hold).` Read that line, not `$?`.
- `Grep` - `@Suppress` returns zero hits across the four files this ticket touched.
- Value equality - `config/detekt/baseline-app_v2.xml` differs from its pre-S1328 state by exactly one deleted line: **12285** lines (was 12286), **3** `StreamInlineAudioManager.kt$` entries (was 4, the `LongParameterList` one pruned), `StreamsActivity.kt` entries untouched. The "byte-identical / 12656 lines" wording predates both the 2026-08-02 baseline rewrite and the INDEX's inverted Completion Gate criterion.
- `Grep` - `Timber.d("S1328:` matches exactly once across `app_v2/src`.

**Status:** `[x]` done

**Step Log:**

- 2026-08-03 - Verification 4/4 PASS, against the corrected predicates above. Gate over all four source paths: `assert-detekt: PASS [app_v2] (no new findings; baselines hold)`, exit 0, re-confirmed inside the Phase 02 `post-change` run. Zero `@Suppress` in the four files. Baseline 12285 lines with 3 entries for this file. One `Timber.d("S1328:` in the tree.

---

## Phase Done Criteria

- [x] Every `Step 03.*` above is `[x] done`.
- [x] Project compiles - `pwsh -NoProfile -File ./a.ps1 fk` exit 0 at the Phase 02 boundary; this phase changed no source, only the regenerated catalog.
- [x] `Grep` for `TODO(phase-03)` returns zero hits.
- [x] `docs/FEATURES*.md` untouched.
- [x] Ticket advanced to `BlockNeedUserTest` with a status note naming both hosts and all five callbacks. It was flipped at the Phase 02 boundary rather than here, because `assert-no-ticket-logs` refuses an `S1328` probe while the ticket is still `In Progress` - the status has to lead the gate.
- [x] Phase-boundary audit - skipped by the protocol's own rule: this phase's Files Touched are a regenerated local index and an append-only journal, no source.

---

## Handoff Notes to Next Phase

Final phase - see INDEX.md Completion Gate. On-device check for the `BlockNeedUserTest` transition:
play an AUDIO channel from the Streams list and from the main-window streams panel, in both
background-audio ON and OFF modes, and confirm the mini-control shows, the now-playing line updates,
the stop button works and a dead URL still raises the retry dialog. The S1328 probe is removed when
the ticket leaves `BlockNeedUserTest`; the S1142, S1219 and S0896 probes stay, because those tickets
are still `BlockNeedUserTest` in their own right.

---

## Rollback Plan

Nothing to roll back - the catalog is a regenerated local index and the dev log is append-only.
