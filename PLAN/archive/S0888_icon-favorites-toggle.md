# Стратегическая спецификация: S0888 - Символ функции у тогглера "Разрешить избранное"

**Ticket:** S0888
**Status:** Archived
**Priority:** 50
**Date:** 2026-07-02
**Tier:** 1 - Quick Win (ad-hoc)
**Roadmap entry:** Ad-hoc - запрос 2026-07-02

<!-- auto-approved by /spec-all - 2026-07-03 -->

---

## 0. Захваченный материал (inbox)

**Захвачено:** 2026-07-02

**Текст:**

Настройки - Общие . У тогглера "Разрешить избранное" по аналогии с прочими добавить символ этой функции (как они приняты у нас в интерфейсе)

---

## 1. Проблема

Тогглер "Разрешить избранное" (Settings > General) единственный в секции без ведущей иконки функции - остальные ряды несут `app:str_icon`. Визуальная несогласованность: ряд читается как менее «настоящий», глазу не за что зацепиться при сканировании списка.

---

## 2. Цели

1. У ряда `rowEnableFavorites` появляется ведущая иконка функции, узнаваемая для «избранного».
2. Паритет portrait/landscape.

**Non-goals:**

- Другие ряды/секции (это отдельные тикеты S0885/S0886/S0887).
- Смена текста, поведения или позиции настройки.

---

## 3. Пожелания и ограничения

### 3.2 Жёсткие ограничения

- **Flavor:** все (ряд в `src/main/res`).
- **API level:** без API-специфики.
- **Wear OS:** не затрагивается.
- **Локализация:** не затрагивается (иконка, не строка).
- **Доступность:** иконка декоративная, ведёт текстовый label - TalkBack читает title, не иконку.

### 3.3 Owner inputs (Approval gate)

- **Related tickets:** S0885, S0886, S0887

---

## 4. Контекст текущей архитектуры

Ряды настроек - `SettingsToggleRow` (кастомный view), иконка задаётся атрибутом `app:str_icon` (`values/attrs.xml`). Секция General описана в `layout/fragment_settings_general.xml` и его landscape-двойнике. Иконочный словарь функций уже устоялся (S0815: `ic_resource_favorites` - канон «избранного», используется в `ResourceIconComposer` и legend).

---

## 5. Предлагаемый подход

Добавить `app:str_icon="@drawable/ic_resource_favorites"` к `rowEnableFavorites` в обоих layout-вариантах - тот же атрибут и тот же drawable, что уже несут соседние ряды и остальной UI для «избранного». Новых ролей/файлов не появляется.

---

## 6. Открытые вопросы / Research items

Открытых вопросов нет - drawable канонизирован S0815 (`ic_resource_favorites`).

---

## 7. Риски

| Риск | Вероятность | Последствия | Митигация |
|------|:-----------:|-------------|-----------|
| Правка только portrait при наличии landscape | Низкая | Рассинхрон ориентаций (Rule 11) | Обе правки в одном изменении, `fr` подтверждает сборку обеих |

---

## 8. Влияние на пользователя (docs/FEATURES)

Без изменений в docs/FEATURES - косметика.

---

## 9. Архитектурные решения (ADR)

ADR нет - решение по устоявшемуся паттерну `str_icon`.

---

## 10. Связи с другими спеками

S0885, S0886, S0887 - аналогичные задачи (узнаваемые иконки у тогглеров настроек, разные разделы). S0815 - канонизировал `ic_resource_favorites`.

---

## 11. Критерии готовности (strategic-level)

1. `rowEnableFavorites` несёт `str_icon` в portrait и landscape.
2. Ресурсная сборка проходит; иконка - канонический `ic_resource_favorites`.
3. Поведение/текст/позиция настройки не изменились.

---

## Last Audit

**Date:** 2026-07-03
**Verdict:** Verified

- Fix: `app:str_icon="@drawable/ic_resource_favorites"` добавлен к `rowEnableFavorites` в `layout/fragment_settings_general.xml:89` и `layout-land/fragment_settings_general.xml:114` - тот же атрибут/drawable, что несут соседние ряды.
- Rule 11 (orientation parity): обе ориентации правлены в одном изменении.
- Rule 22: манифест настроек не трогается - иконка не меняет presence/behavior/position/naming ряда (settings-doc-sync gate подтверждает).
- Валидация: `.\a.ps1 fr` - expected: BUILD SUCCESSFUL | actual: см. dev-log; post-change `-ScopeToFile` (Xml) - gate-набор PASS.
