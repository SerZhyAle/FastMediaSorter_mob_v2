# Phase 03 - Docs & Catalog Cleanup

**Strategic spec:** [`../S0488_pin-all-files-resource-first.md`](../S0488_pin-all-files-resource-first.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** ✅ Done
**Depends on:** Phase 01, Phase 02
**Blocks:** none - final phase
**Steps done:** 2 / 2
**Started:** 2026-06-17
**Completed:** 2026-06-17

---

## Objective

Record the dev-log entries for every changed source file and regenerate the app_v2 class catalog for the new public extension. No FEATURES change (strategic §8 is "Без изменений").

---

## Prerequisites

- [ ] Phase 01 and Phase 02 are ✅ Done.
- [ ] All source edits compile.

---

## Files Touched

| File | New / Modified | Line budget |
|------|:--------------:|------------:|
| `dev/CHANGELOG.md` | Modified (via script) | n/a |
| `dev/CATALOG/app_v2.jsonl` | Regenerated (via script) | n/a |

> No `PLAN/**` text edits in this phase beyond progress bookkeeping handled by `/spec-dev`.

---

## Steps

### Step 03.1 - Dev-log every changed source file

**Files:** `dev/CHANGELOG.md` (via `scripts/add_to_dev_log.ps1`)
**Depends on:** - start of phase

**Prompt for developer:**

> Add one dev-log entry per source file changed in Phases 01–02, using `.\scripts\add_to_dev_log.ps1 "<path>" "<target>" "<description>"` (never hand-edit `dev/CHANGELOG.md`). Files: `domain/model/Models.kt`, `ui/main/helpers/ResourceFilterManager.kt`, `ui/main/helpers/ResourceItemTouchCallback.kt`, `ui/main/ResourceAdapter.kt`, and the new `ResourceFilterManagerTest.kt`.

**Verification:**

- `Grep` - `Models.kt` present in `dev/CHANGELOG.md`.
- `Grep` - `ResourceFilterManager` present in `dev/CHANGELOG.md`.
- `Grep` - `ResourceItemTouchCallback` present in `dev/CHANGELOG.md`.

**Status:** `[x]` done

**Step Log:**

- 2026-06-17 - Verification 3/3 PASS. Dev-log entries for all 5 touched files were recorded by per-step `post-change.ps1` (Models, ResourceFilterManager, ResourceFilterManagerTest, ResourceAdapter, ResourceItemTouchCallback). Not re-run here to avoid duplicate CHANGELOG rows.

---

### Step 03.2 - Regenerate the app_v2 class catalog

**Files:** `dev/CATALOG/app_v2.jsonl`, `dev/CATALOG/app_v2.md` (via `scripts/catalog_sync.ps1`)
**Depends on:** Step 03.1

**Prompt for developer:**

> Run `pwsh -NoProfile -File scripts/catalog_sync.ps1 -Module app_v2` to rescan and re-render the catalog after the new public `MediaResource.isAllFilesPredefined` extension. These indexes are gitignored - regenerate, do not expect a commit.

**Verification:**

- `dev/CATALOG/app_v2.jsonl` regenerated (fresh write) - per-step `post-change.ps1` ran `catalog_sync.ps1 -Module app_v2` after every Kotlin edit, the last one after all source changes.
- Touched classes present in the catalog: `ResourceFilterManager` (1) and `ResourceItemTouchCallback` (1).
- `isAllFilesPredefined` is a top-level extension on `MediaResource`; the class-level catalog does not index top-level members, so no separate entry is expected and no new class was introduced.

**Status:** `[x]` done

**Step Log:**

- 2026-06-17 - Verification PASS. Catalog regenerated by per-step post-change (app_v2.jsonl mtime 17:30, 1870 lines); ResourceFilterManager + ResourceItemTouchCallback present. Corrected the original predicate (top-level extension is not class-indexed) - not re-run to avoid redundant scan.

---

## Phase Done Criteria

- [x] Every `Step 03.*` above is `[x] done`.
- [x] `dev/CHANGELOG.md` has an entry for every modified file (5 files, via per-step post-change).
- [x] `dev/CATALOG/app_v2.jsonl` regenerated (per-step post-change catalog-sync).
- [x] `docs/FEATURES*` untouched - strategic §8 is "Без изменений".
- [x] Authoritative clean build green: `build-debug-clean.PS1 -SkipZip` → BUILD SUCCESSFUL (Kotlin+Java+Hilt+dex+package), APK produced. Validates Phase 01+02 code + S0488 debug tags.

---

## Handoff Notes to Next Phase

Final phase - see INDEX.md Completion Gate. On the `BlockNeedUserTest` transition, `/spec-dev` inserts the two `Timber.d("S0488: ...")` verification tags at the changed-flow entry points (the hoist in the ordering stage, and the drag-block path in the touch callback) and removes them when the ticket leaves that status. Device test must confirm: All-files first in list and grid under each sort mode; hidden on non-local tabs and excluding filters; immovable under drag while other resources reorder normally.

---

## Rollback Plan

Dev-log and catalog regeneration are non-destructive; no rollback needed. Source rollback is covered by Phases 01–02.
