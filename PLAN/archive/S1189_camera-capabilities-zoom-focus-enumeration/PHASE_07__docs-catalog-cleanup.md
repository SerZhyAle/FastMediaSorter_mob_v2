# Phase 07 - Docs, catalog and inventory cleanup

**Strategic spec:** [`../S1189_camera-capabilities-zoom-focus-enumeration.md`](../S1189_camera-capabilities-zoom-focus-enumeration.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** ✅ Done
**Depends on:** Phase 01, Phase 02, Phase 03, Phase 04, Phase 05, Phase 06
**Steps done:** 4 / 4
**Started:** 2026-07-25
**Completed:** 2026-07-25

---

## Step Log (phase level)

- 2026-07-25 - 07.1 PASS: catalog regenerated by the closure facade; `role` + `status=new` set for all five new classes (`CameraHardwareInventory`, `CameraHardwareDataSource`, `GatherCameraDiagnosticsUseCase`, `CameraLensEntry`, `CameraLensEnumerationManager`), verified by reading `dev/CATALOG/app_v2.jsonl` back. `status=new` rather than `tested` - the code compiles and passes the static gates but has never run on a device.
- 2026-07-25 - 07.2 PASS: two records, because the ticket delivers two distinct user-visible capabilities - `camera.device-matched-lenses-zoom-range-and-photo-sizes` (CHANGE) and `camera.camera-hardware-report` (ADD). Flavours read off the real gate: the capture screen carries no `BuildConfig` flag and has no flavour source set, so it ships in all six. `validate.ps1` PASS (580 records).
- 2026-07-25 - 07.3 PASS: `assert-no-ticket-logs.ps1` expected 0 | actual 0 after the status flip. The gate had reported the four probes as stale beforehand purely because the ticket was still `In Progress` - the status must move first, then the gate is meaningful.
- 2026-07-25 - 07.4 PASS: journalled through `close-and-log.ps1` in one process (status + 4 dev logs + feature record + catalog scan/render).

---

## Files Touched

| File | New / Modified | Line budget |
|------|:--------------:|------------:|
| `dev/CATALOG/app_v2.jsonl` | Regenerated | - |
| `docs/ALL_FEATURES.jsonl` | Modified | - |
| `dev/CHANGELOG.md` | Modified (via script only) | - |

---

## Objective

Record the delivered capability, refresh the generated indexes and confirm no ticket-scoped debug residue is left behind.

---

## Prerequisites

- [ ] Phases 01-06 are ✅ Done.

---

## Steps

### Step 07.1 - Regenerate the class catalog

**Files:** `dev/CATALOG/app_v2.jsonl`
**Depends on:** - start of phase

**Prompt for developer:**

> Run `pwsh -NoProfile -File scripts/catalog_sync.ps1 -Module app_v2` once for the whole ticket, then set `role` and `status` for the classes added in Phases 01-02 via `dev/CATALOG/scripts/set.ps1`.

**Verification:**

- Run `pwsh -NoProfile -File dev/CATALOG/scripts/query.ps1 -ClassMatches "CameraLensEnumerationManager"` - one record returned with a non-empty `role`.
- Run `pwsh -NoProfile -File dev/CATALOG/scripts/query.ps1 -ClassMatches "CameraHardwareDataSource"` - one record returned with a non-empty `role`.

**Status:** `[ ]` not done

---

### Step 07.2 - Record the capability in the feature inventory

**Files:** `docs/ALL_FEATURES.jsonl`
**Depends on:** Step 07.1

**Prompt for developer:**

> Add one record via `pwsh -NoProfile -File scripts/all_features/add.ps1` for the delivered capability, in English. Read the shipping flavors off the actual gate: the capture screen lives in `src/main` and carries no `BuildConfig` flavor flag, so the record covers every build that ships the camera screen - verify that list against `app_v2/build.gradle.kts` rather than copying a sibling record, then read the written record back and confirm the flavor field matches what you verified.

**Verification:**

- `Grep` - `S1189` matches at least once in `docs/ALL_FEATURES.jsonl`.
- Run `pwsh -NoProfile -File scripts/all_features/validate.ps1` - exit 0.

**Status:** `[ ]` not done

---

### Step 07.3 - Confirm no ticket residue in shipped code

**Files:** `app_v2/src/main/java/com/sza/fastmediasorter/`
**Depends on:** Step 07.2

**Prompt for developer:**

> Confirm the only `S1189:` occurrences in `.kt` sources are the temporary `Timber.d` verification probes required while the ticket sits in `BlockNeedUserTest`, and that no permanent log line carries the ticket id. Grep for the bare quoted prefix too - a probe wrapped across lines by the formatter still has to be found.

**Verification:**

- `Grep` - `S1189` in `app_v2/src/**/*.kt` returns only `Timber.d` probe lines.
- Run `pwsh -NoProfile -File scripts/quality/assert-no-ticket-logs.ps1` - exit 0.

**Status:** `[ ]` not done

---

### Step 07.4 - Journal the ticket

**Files:** `dev/CHANGELOG.md`
**Depends on:** Step 07.3

**Prompt for developer:**

> Add the dev-log entries for the ticket in one batched call via `scripts/spec_catalog/close-and-log.ps1 -DevLogs '<json-array>'`, never by editing `dev/CHANGELOG.md` directly. One entry per logical change, not per touched file.

**Verification:**

- `Grep` - `S1189` matches at least once in `dev/CHANGELOG.md`.

**Status:** `[ ]` not done

---

## Phase Done Criteria

- [ ] Every `Step 07.*` above is `[x] done`.
- [ ] `Grep` for `TODO(phase-07)` returns zero hits.
- [ ] `docs/FEATURES*.md` left untouched - the showcase is `/skill-release`-owned.

---

## Handoff Notes to Next Phase

Final phase - see INDEX.md Completion Gate.

---

## Rollback Plan

Regenerated indexes and journal entries only - revert the commit; no runtime surface is affected.
