# Phase 05 — Docs, catalog, dev log cleanup

**Strategic spec:** [`../S0137_feature-cast-network-cloud-streaming.md`](../S0137_feature-cast-network-cloud-streaming.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** ⬜ Not started
**Depends on:** Phase 03, Phase 04
**Blocks:** none — final phase
**Steps done:** 0 / 4
**Started:** —
**Completed:** —

---

## Objective

Bring user-facing documentation, file/class catalog, dev changelog, and the strategic spec to a Verified-ready state. After this phase `/spec-check S0137` should return `Verified`.

---

## Prerequisites

- [ ] Phase 03 is ✅ Done (or explicitly skipped with a note in INDEX Blockers Log).
- [ ] Phase 04 is ✅ Done (or explicitly skipped with a note in INDEX Blockers Log).
- [ ] All `Timber.d("S0137:` tags from earlier phases are still in place — they will be removed atomically by `/spec-check` on Verified transition, not here.

---

## Files Touched

| File | New / Modified | Line budget |
|------|:--------------:|------------:|
| `docs/FEATURES.md` | Modified | append/edit Cast section |
| `docs/FEATURES_RU.md` | Modified | append/edit Cast section |
| `docs/FEATURES_UK.md` | Modified | append/edit Cast section |
| `dev/CATALOG/app_v2.jsonl` | Modified | regenerated by scan.ps1 |
| `dev/CATALOG/app_v2.md` | Modified | regenerated by render.ps1 |
| `dev/CHANGELOG.md` | Modified | append (via `add_to_dev_log.ps1` only) |

---

## Steps

### Step 05.1 — Update `docs/FEATURES.md` + RU + UK with the network/cloud Cast capability

**Files:** `docs/FEATURES.md`, `docs/FEATURES_RU.md`, `docs/FEATURES_UK.md`
**Depends on:** — start of phase

**Prompt for developer:**

> Locate the Cast section (or its closest analogue: media output / Chromecast). Add a concise bullet to each of the three language mirrors:
>
> - EN: "Cast SMB / SFTP / FTP / Cloud media to Chromecast — files are downloaded to the per-session cache and served via the local proxy."
> - RU: "Каст медиа из SMB / SFTP / FTP / облака на Chromecast — файлы скачиваются в кеш сессии и отдаются через локальный proxy."
> - UK: "Каст медіа з SMB / SFTP / FTP / хмари на Chromecast — файли завантажуються в кеш сесії та віддаються через локальний proxy."
>
> If a Cast section does not yet exist in any of the three files, create one named "Chromecast" / "Chromecast" / "Chromecast" and place it in the same ordinal position across all three files. Wording must pass `docs/COMMUNICATION_POLICY.md` §6 tone checklist.

**Verification:**

- `Grep` — phrase `SMB / SFTP / FTP` appears exactly once in `docs/FEATURES.md`.
- `Grep` — phrase `SMB / SFTP / FTP` appears exactly once in each of `_RU.md` and `_UK.md`.
- All three files have parallel Cast sections (manual visual check).

**Status:** `[ ]` not done

---

### Step 05.2 — Regenerate the app_v2 catalog

**Files:** `dev/CATALOG/app_v2.jsonl`, `dev/CATALOG/app_v2.md`
**Depends on:** Step 05.1

**Prompt for developer:**

> Run, in order:
>
> ```powershell
> pwsh -File dev/CATALOG/scripts/scan.ps1 -Module app_v2
> pwsh -File dev/CATALOG/scripts/render.ps1 -Module app_v2
> ```
>
> If `CastMediaManager.kt` shows `role: -` or `status: unknown` after scan, fill them via `set.ps1` (role: `cast`, status: `active`). Commit both regenerated files together with the code in the final phase commit.

**Verification:**

- `Grep` — entry for `CastMediaManager` exists in `dev/CATALOG/app_v2.jsonl` with the new line count after Phases 02/03/04.
- `Glob` — `dev/CATALOG/app_v2.md` modification timestamp is newer than the last code change.

**Status:** `[ ]` not done

---

### Step 05.3 — Add dev-changelog entries for every file touched in S0137

**Files:** `dev/CHANGELOG.md` (via `add_to_dev_log.ps1`)
**Depends on:** Step 05.2

**Prompt for developer:**

> For each modified or new file across all S0137 phases that does not yet have a dev-log entry, run:
>
> ```powershell
> .\scripts\add_to_dev_log.ps1 "<path>" "S0137" "<short description>"
> ```
>
> Coverage list (minimum):
>
> - `app_v2/.../helpers/CastMediaManager.kt` — "Delegate remote download to NetworkFileManager + size gates"
> - `app_v2/.../PlayerManagerInitializer.kt` — "Pass networkFileManager into CastMediaManager"
> - `app_v2/src/main/res/values/strings.xml` — "Cast oversize toasts (audio/image)"
> - `app_v2/src/main/res/values-ru/strings.xml` — same
> - `app_v2/src/main/res/values-uk/strings.xml` — same
> - `docs/FEATURES.md` + RU + UK — "Document Chromecast support for SMB/SFTP/FTP/Cloud"

**Verification:**

- `Grep` — `S0137` appears in `dev/CHANGELOG.md` with at least one entry per file in the coverage list.

**Status:** `[ ]` not done

---

### Step 05.4 — Final string locale audit and self-check

**Files:** none — verification only
**Depends on:** Step 05.3

**Prompt for developer:**

> Run the locale audit and the spec audit:
>
> ```powershell
> pwsh -File scripts/check_strings_localized.ps1 -KeyPrefix "cast_"
> ```
>
> Then invoke `/spec-check S0137`. Address any findings via `/spec-fix S0137` before declaring the phase done. Do not transition the spec to `Verified` here — that is `/spec-check`'s job.

**Verification:**

- `pwsh -File scripts/check_strings_localized.ps1 -KeyPrefix "cast_"` — exit code 0.
- `/spec-check S0137` — final summary block in the spec's `## Last Audit` shows zero unresolved findings.

**Status:** `[ ]` not done

---

## Phase Done Criteria

- [ ] Every `Step 05.*` above is `[x] done`.
- [ ] `Grep` for `TODO(phase-05)` returns zero hits.
- [ ] Strategic spec `Status:` advanced to `Verified` by `/spec-check`.
- [ ] All `Timber.d("S0137:` tags removed by `/spec-check` Verified-transition (not by this phase).

---

## Handoff Notes to Next Phase

Final phase — see [`INDEX.md`](INDEX.md) Completion Gate.

---

## Rollback Plan

Documentation rollback: revert the FEATURES + catalog + CHANGELOG commits. No runtime impact.
