# Phase 03 - Regression cleanup

**Strategic spec:** [`../S1369_bulk-delete-dies-with-hosting-screen.md`](../S1369_bulk-delete-dies-with-hosting-screen.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** Not started
**Depends on:** Phase 02
**Blocks:** none
**Steps done:** 0 / 3

## Objective

Prove regression coverage, close generated metadata and leave the ticket ready for device verification.

## Files Touched

| File | Change | Line budget |
|---|:---:|---:|
| `app_v2/src/test/java/com/sza/fastmediasorter/ui/browse/managers/BrowseDeleteManagerTest.kt` | New or Modified | <= 500 |
| `app_v2/src/test/java/com/sza/fastmediasorter/worker/BrowseFileTransferWorkerDeleteTest.kt` | New | <= 500 |
| `dev/CHANGELOG.md` | Generated | n/a |

## Steps

### Step 03.1 - Add delete routing tests

**Files:** `BrowseDeleteManagerTest.kt`, `BrowseFileTransferWorkerDeleteTest.kt`
**Depends on:** Phase 02

**Prompt for developer:**

> Test DELETE request construction, active-transfer refusal, file/directory aggregation and representative terminal mappings. Use fakes rather than WorkManager or network I/O.

**Why:**

The strategic criteria require the durable path to preserve delete semantics under lifecycle loss, and deterministic tests cover its data contract.

**Verification:**

- Tests assert DELETE request fields, soft-delete policy and source directory markers.
- Tests assert terminal success, partial failure and cancellation mapping.

**Status:** `[ ]` not done

### Step 03.2 - Run static and build proof

**Files:** all changed source and tests
**Depends on:** Step 03.1

**Prompt for developer:**

> Run scoped closure plus the fast Kotlin build and targeted unit tests. Record expected and actual exit values.

**Why:**

The change alters coroutine, worker and persisted-contract code, so static checks alone do not prove compilation or tests.

**Verification:**

- `post-change.ps1 -ScopeToFile` passes for the complete changed set.
- `a.ps1 fk` passes.
- Targeted DELETE/transfer unit tests pass.

**Status:** `[ ]` not done

### Step 03.3 - Close documentation and manual device gate

**Files:** strategic and tactical spec metadata, dev log
**Depends on:** Step 03.2

**Prompt for developer:**

> Regenerate the Kotlin catalog if needed, add the single ticket dev-log entry, mark phase metadata, and add temporary `Timber.d("S1369: ...")` probes only if on-device validation remains required.

**Why:**

This lifecycle regression needs a real slow remote-resource run in addition to automated proof; temporary probes make the changed flow observable without permanent ticket logging.

**Verification:**

- Catalog is synchronized if Kotlin declarations changed.
- Dev log is generated by its script.
- Device test instructions cover 50+ remote files, Back/rotation and notification reattach.

**Status:** `[ ]` not done

## Phase Done Criteria

- [ ] Every step is done.
- [ ] Full audit follows `docs/CODE_AUDIT_PROTOCOL.md` layers 1-3 and 7.
- [ ] `/spec-check S1369` records the final verdict.
