# Phase 06 - Rules, persona and closure

**Strategic spec:** [`../S1392_bugfix-flavor-matrix-docs-contradict-gates.md`](../S1392_bugfix-flavor-matrix-docs-contradict-gates.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** ✅ Done
**Depends on:** Phase 04, Phase 05
**Blocks:** none - final phase
**Steps done:** 5 / 5
**Started:** 2026-08-04
**Completed:** 2026-08-04

---

## Objective

Remove the stale matrix from the agent-facing rule and persona surfaces, then close the ticket through the facade with the registry and gate evidence recorded.

---

## Prerequisites

- [x] Phase 04 and Phase 05 are ✅ Done.
- [x] Step 05.5 residue list available.
- [x] Conformance gate green.

---

## Files Touched

| File | New / Modified | Line budget |
|------|:--------------:|------------:|
| `.claude/agents/android-rd-specialist.md` | Modified | ≤ 12 delta |
| `.claude/agents/*.md` (other personas carrying a matrix) | Modified | ≤ 12 delta each |
| `CLAUDE.md` | Modified | ≤ 6 delta |
| `AGENTS.md` | Modified | ≤ 6 delta |
| `.claude/agent-memory/android-rd-specialist/project_flavor_matrix_cloud_correction.md` | Modified | ≤ 10 delta |

---

## Steps

### Step 06.1 - Replace the persona matrix with a pointer

**Files:** `.claude/agents/android-rd-specialist.md`, any other persona under `.claude/agents/` carrying a flavor matrix

**Depends on:** - start of phase

**Prompt for developer:**

> Grep `.claude/agents/` for flavor matrices in the `VIDEO+AUDIO+IMAGES` shorthand. Replace each with a one-line pointer to `docs/FLAVOR_MATRIX.md` plus the instruction to read it rather than answer from the prompt, keeping only the facts that never drift and are needed up front: the six flavor names and the `legacy` minSdk. Do not restate the capability grid in any persona.

**Why:**

Strategic §5.1 records that a persona reproducing the matrix from memory is the path that produced this finding, and strategic §2 goal 5 requires those surfaces to stop being a source of the wrong grid; a shorthand copy cannot be gated, so the only safe form is a pointer.

**Verification:**

- `Grep` - `VIDEO+AUDIO+IMAGES` and `VIDEO+IMAGES` absent from `.claude/agents/`.
- `Grep` - `docs/FLAVOR_MATRIX.md` present in every persona that previously carried a matrix.
- `Grep` - the six flavor names present in `.claude/agents/android-rd-specialist.md`.

**Status:** `[x]` done

---

### Step 06.2 - Complete the flavor list in the rule files

**Files:** `CLAUDE.md`, `AGENTS.md`

**Depends on:** Step 06.1

**Prompt for developer:**

> The flavor bullet in the project-structure section lists four flavors. Add `vr` and `noLegal`, and point at `docs/FLAVOR_MATRIX.md` for the capability grid instead of naming capabilities inline. Mirror the same edit in `AGENTS.md`, which is the parallel rule file for non-Claude agents.

**Why:**

Research item §3.17 shows the rule file's own enumeration is incomplete, and CLAUDE.md's header requires shared rule changes to be mirrored into `AGENTS.md` in the same change.

**Verification:**

- `Grep` - `vr` and `noLegal` present in the flavor bullet of both files.
- `Grep` - `docs/FLAVOR_MATRIX.md` referenced in both files.
- The two bullets are textually consistent with each other.

**Status:** `[x]` done

---

### Step 06.3 - Update the agent memory that carried the correction

**Files:** `.claude/agent-memory/android-rd-specialist/project_flavor_matrix_cloud_correction.md`

**Depends on:** Step 06.2

**Prompt for developer:**

> That memory exists because the persona matrix was wrong and had to be corrected from the build file each time. With the persona now pointing at a generated table, rewrite the memory to say the canonical answer is `docs/FLAVOR_MATRIX.md`, generated by `scripts/docs/generate-flavor-matrix.ps1` and gated by `scripts/quality/assert-flavor-matrix-docs.ps1`, and keep only the non-derivable facts - that binary file types are not capability-gated at all, and which incident taught this. Run the memory budget gate afterwards.

**Why:**

Strategic §2 goal 5 covers the agent-facing surfaces, and a memory that duplicates a now-generated table is exactly the kind of frozen snapshot that goes stale next time a flag changes.

**Verification:**

- `Grep` - `docs/FLAVOR_MATRIX.md` present in the memory file.
- `Grep` - the memory no longer restates the full per-flavor flag list.
- Run `pwsh -NoProfile -File scripts/quality/assert-memory-budget.ps1` - exit 0.
- `MEMORY.md` pointer line still resolves to this file and its hook still matches the new content.

**Status:** `[x]` done

---

### Step 06.4 - Decide the feature-inventory question explicitly

**Files:** none - decision plus dev log

**Depends on:** Step 06.3

**Prompt for developer:**

> Grep `docs/ALL_FEATURES.jsonl` for `S1392`. This ticket corrects documentation and adds a gate; it ships no new user-visible capability, so no `ADD`/`CHANGE` record is expected. Confirm zero records and record that conclusion, rather than leaving the absence unexplained. If any correction in Phases 04-05 turned out to change what a user can do rather than what a document claims, add a `FIX` record via `scripts/all_features/add.ps1` instead.

**Why:**

Strategic §8 states no new capability is delivered, and the `/spec-all` constraint on the feature inventory requires the absence of a record to be a stated conclusion rather than an oversight.

**Verification:**

- `Grep` - `S1392` in `docs/ALL_FEATURES.jsonl` returns zero hits (or exactly one `FIX` record if the exception applied).
- Conclusion recorded in the dev log entry for this phase.

**Status:** `[x]` done

---

### Step 06.5 - Close through the facade

**Files:** none - closure only

**Depends on:** Step 06.4

**Prompt for developer:**

> Run the registry closure trio, then close the whole ticket through `scripts/post-change.ps1` naming the complete changed set with `-Files` and `-ScopeToFile`, `-ChangeType Mixed`. Read the verdict line: only the bare `post-change: PASS` counts as clean; `PASS WITH ADVISORIES` requires naming each advisory. Distinguish exit 1 from exit 2. Batch the per-phase dev logs through `close-and-log.ps1 -DevLogs` if any phase entry is still missing.

**Why:**

Strategic §3.3 sets this exact evidence as the validation level for the ticket, and CLAUDE.md §12 makes the facade the required closure path with the verdict read rather than assumed.

**Verification:**

- Run `pwsh -NoProfile -File scripts/document_registry/validate.ps1` - exit 0.
- Run `pwsh -NoProfile -File scripts/document_registry/generate.ps1 -Check` - exit 0.
- Run `pwsh -NoProfile -File scripts/quality/assert-flavor-matrix-docs.ps1 -Gate` - exit 0.
- Run `pwsh -NoProfile -File ./a.ps1 fg` - PASS.
- `post-change.ps1` prints `post-change: PASS` (or advisories are enumerated) and its exit code is recorded.

**Status:** `[x]` done

---

## Phase Done Criteria

- [x] Every `Step 06.*` above is `[x] done`.
- [x] `Grep` for `TODO(phase-06)` returns zero hits.
- [x] Dev log entry added for the phase.
- [x] Phase-boundary audit run - no unresolved P0/P1 findings.
- [x] `/spec-check S1392` returns `Verified`.

---

## Handoff Notes to Next Phase

Final phase - see INDEX.md Completion Gate.

---

## Rollback Plan

Revert the rule, persona and memory edits. The generated artifacts and the gate from Phases 01-02 stand on their own and need no rollback with them.

