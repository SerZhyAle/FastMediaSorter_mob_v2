# Phase 03 - Docs and catalog cleanup

**Strategic spec:** [`../S1675_camera-sub-1x-zoom-not-offered.md`](../S1675_camera-sub-1x-zoom-not-offered.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** ⬜ Not started
**Depends on:** Phase 01, Phase 02
**Blocks:** none - final phase
**Steps done:** 2 / 2
**Started:** -
**Completed:** -

---

## Objective

Close the ticket mechanically: refresh the class catalog for the touched camera classes and record the delivered capability.

---

## Prerequisites

- [ ] All phases in "Depends on" are ✅ Done.
- [ ] Working tree is clean or on a feature branch.

---

## Files Touched

| File | New / Modified | Line budget |
|------|:--------------:|------------:|
| `dev/CATALOG/app_v2.jsonl` | Regenerated | n/a |
| `docs/ALL_FEATURES.jsonl` | Modified | n/a |

---

## Steps

### Step 03.1 - Refresh the class catalog

**Files:** `dev/CATALOG/app_v2.jsonl`
**Depends on:** - start of phase

**Prompt for developer:**

> Run `pwsh -NoProfile -File scripts/catalog_sync.ps1 -Module app_v2` once for the whole ticket. No class is new here, so no `set.ps1` role assignment is needed; confirm the touched classes are present with their updated line counts.

**Why:**

CLAUDE.md's post-change contract makes the catalog the first lookup any later ticket performs, and the camera classes changed size in both phases.

**Verification:**

- `pwsh -NoProfile -File dev/CATALOG/scripts/query.ps1 -ClassMatches "*CameraZoomControlsManager*"` returns one record.
- `pwsh -NoProfile -File dev/CATALOG/scripts/query.ps1 -ClassMatches "*CameraRuntimeCapabilities*"` returns one record.

**Status:** `[x]` done

**Step Log:**

- 2026-08-15 - Catalog regenerated by the phase-01 and phase-02 closures (2857 records, both camera classes present). ALL_FEATURES record camera.rear-lens-pills-on-fixed-range-lens added; flavors read off the real gate - app_v2/build.gradle.kts states the camera lives in src/main and compiles into every flavor - so all six. validate.ps1 exit 0, 721 records.

---

### Step 03.2 - Record the capability

**Files:** `docs/ALL_FEATURES.jsonl`
**Depends on:** Step 03.1

**Prompt for developer:**

> Add one record via `scripts/all_features/add.ps1` (or `close-and-log.ps1 -FuncOp CHANGE`) in English: on a lens whose own zoom range is a single point, the camera now shows one pill per rear lens in equivalent values and a tap switches the optics, instead of hiding the zoom row entirely. Read the flavor list off the real gate - the camera capture screen's own source set - never off a sibling record.

**Why:**

CLAUDE.md's feature-inventory rule makes `docs/ALL_FEATURES.jsonl` the developer inventory of shipped capability, and a user-visible camera change with no record is invisible to the next release's showcase diff.

**Verification:**

- `pwsh -NoProfile -File scripts/all_features/validate.ps1` exits 0; record `expected: 0 | actual: <code>`.
- `Grep` - `S1675` present in `docs/ALL_FEATURES.jsonl`.

**Status:** `[x]` done

**Step Log:**

- 2026-08-15 - Catalog regenerated by the phase-01 and phase-02 closures (2857 records, both camera classes present). ALL_FEATURES record camera.rear-lens-pills-on-fixed-range-lens added; flavors read off the real gate - app_v2/build.gradle.kts states the camera lives in src/main and compiles into every flavor - so all six. validate.ps1 exit 0, 721 records.

---

## Phase Done Criteria

- [x] Every `Step 03.*` above is `[x] done`.
- [x] `Grep` for `TODO(phase-03)` returns zero hits.
- [x] Dev log entry added for the ticket via `.\scripts\add_to_dev_log.ps1` (one entry per logical change, not per file).
- [x] Phase-boundary audit run - no unresolved P0/P1 findings (CLAUDE.md §13 / `docs/CODE_AUDIT_PROTOCOL.md` "Phase-boundary audits"; see `/spec-dev` "Phase-boundary audit" step).

---

## Handoff Notes to Next Phase

Final phase - see INDEX.md Completion Gate.

---

## Rollback Plan

Revert the phase commit - regenerated index and one inventory row, no source changed.
