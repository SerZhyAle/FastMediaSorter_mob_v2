# Phase 06 - Docs and catalog cleanup

**Strategic spec:** [`../S1637_activity-logic-debt-player-hosts.md`](../S1637_activity-logic-debt-player-hosts.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** ✅ Done
**Depends on:** Phase 05
**Blocks:** none - final phase
**Steps done:** 3 / 3
**Started:** 2026-08-14
**Completed:** 2026-08-14

---

## Objective

Lower the violation gate to zero, refresh the class catalog, and record the change.

---

## Prerequisites

- [ ] All phases in "Depends on" are ✅ Done.
- [ ] Working tree is clean or on a feature branch.

---

## Files Touched

| File | New / Modified | Line budget |
|------|:--------------:|------------:|
| `scripts/quality/assert-activity-logic-not-growing.ps1` | Modified | ≤ 5 changed lines |

> The catalog index and the changelog are regenerated, not hand-edited.

---

## Steps

### Step 06.1 - Ratchet the gate to zero

**Files:** `scripts/quality/assert-activity-logic-not-growing.ps1`
**Depends on:** - start of phase

**Prompt for developer:**

> Lower the baseline from 32 to 0 in the gate's own baseline declaration, the same way S1329 lowered it from 78 to 32. Do not add an exclusion for either host file.

**Why:**

Strategic §11 criterion 2 requires the gate itself to be at zero, not merely the source count, so that a reintroduced field fails a build rather than sitting unnoticed.

**Verification:**

- `Grep` - the baseline value in the gate file reads `0`.
- `pwsh -NoProfile -File scripts/quality/assert-activity-logic-not-growing.ps1` reports `baseline 0 | actual 0 | delta 0`.
- `Grep` - neither host file name appears as an exclusion in the gate script.

**Status:** `[x]` done

**Step Log:**

- 2026-08-14 - No hand edit was needed: the baseline lives in scripts/quality/activity-logic-baseline.txt and the gate ratchets it down on its own, so it fell 32 -> 31 -> 23 -> 17 -> 6 -> 0 as phases 01, 03, 04 and 05 landed. Verified: the baseline file reads 0; the gate under -Gate reports 'baseline 0 | actual 0 | delta 0' and exits 0; neither host file name appears anywhere in the gate script, and the single mention of PlayerActivity in the shared matcher library is a comment explaining why the regex must handle wrapped and internal declarations - not an exclusion.

---

### Step 06.2 - Regenerate the class catalog

**Files:** `dev/CATALOG/app_v2.jsonl`
**Depends on:** Step 06.1

**Prompt for developer:**

> Run `pwsh -NoProfile -File scripts/catalog_sync.ps1 -Module app_v2` so the new `ImageEditFactory` is indexed, then set its role and status with `set.ps1`.

**Why:**

Strategic §5.3 expects a later screen to find the shared supplier rather than build a third copy, and an unindexed class is not findable by the catalog-first research order.

**Verification:**

- `pwsh -NoProfile -File dev/CATALOG/scripts/query.ps1 -ClassMatches "ImageEditFactory"` returns exactly one record.

**Status:** `[x]` done

**Step Log:**

- 2026-08-14 - Catalog regenerated by the phase closures (2846 records) and roles set: ImageEditFactory as the shared nine-use-case supplier, PlayerHostFactory plus PlayerHostRepositories as the non-cluster supplier, all three status=new. Verified: query.ps1 -ClassMatches ImageEditFactory returns exactly one record, now carrying its role.

---

### Step 06.3 - Close the change through the facade

**Files:** all files touched by phases 01-06
**Depends on:** Step 06.2

**Prompt for developer:**

> Run `scripts/post-change.ps1` naming the whole changed set with `-ScopeToFile` and `-ChangeType Mixed`. Skip `docs/FEATURES*.md` - strategic §8 reads "Без изменений в docs/FEATURES."

**Why:**

Strategic §2 goal 4 states behaviour does not change, so there is no user-visible capability to record; the closure still owes a changelog row and the quality gates.

**Verification:**

- `post-change: PASS` printed, exit code 0.
- `dev/CHANGELOG.md` carries a row for every logical change of this ticket.

> **Correction at implementation time.** The original predicate read "carries one row for this ticket" and assumed a single closure at the end. `/spec-dev` closes per phase instead - each phase's own Done Criteria demand a dev-log entry - so phases 01-02, 03, 04 and 05 each wrote their row as they landed. This step adds the one remaining unjournalled change, the ratcheted gate baseline. Five rows is the correct outcome; collapsing them would have meant skipping four phase gates.

**Status:** `[x]` done

**Step Log:**

- 2026-08-14 - post-change PASS exit 0 on the last unjournalled change, the ratcheted gate baseline. The four source closures already ran per phase (19:18, 19:23, 19:29, 19:46), so dev/CHANGELOG.md now carries five rows for this ticket - the step's original one-row predicate assumed a single end-of-ticket closure and is corrected above. docs/FEATURES untouched per strategic section 8.

---

## Phase Done Criteria

- [x] Every `Step 06.*` above is `[x] done`.
- [x] Project compiles - run `/build` (do not invoke gradle directly).
- [x] `Grep` for `TODO(phase-06)` returns zero hits.
- [x] Phase-boundary audit run - no unresolved P0/P1 findings.

---

## Handoff Notes to Next Phase

Final phase - see INDEX.md Completion Gate.

---

## Rollback Plan

Revert the baseline change alone if the count proves wrong; the source phases roll back independently.
