# Phase 04 - Docs and catalog cleanup

**Strategic spec:** [`../S1398_hairline-background-progress-bar.md`](../S1398_hairline-background-progress-bar.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** ✅ Done
**Depends on:** Phase 01, Phase 02, Phase 03
**Blocks:** none - final phase
**Steps done:** 3 / 3
**Started:** 2026-08-10
**Completed:** 2026-08-10

---

## Objective

Register the delivered capability, refresh the class catalog for the three new classes, and close the ticket through the mechanical facade.

---

## Prerequisites

- [ ] All phases in "Depends on" are ✅ Done.
- [ ] Working tree carries only this ticket's changes, or the closure names the whole changed set.

---

## Files Touched

| File | New / Modified | Line budget |
|------|:--------------:|------------:|
| `docs/ALL_FEATURES.jsonl` | Modified via CLI | +1 record |
| `dev/CATALOG/app_v2.jsonl` + `.md` | Regenerated | n/a |
| `dev/CHANGELOG.md` | Appended via CLI | +1 row |

---

## Steps

### Step 04.1 - Record the delivered capability

**Files:** `docs/ALL_FEATURES.jsonl`
**Depends on:** - start of phase

**Prompt for developer:**

> Add one record through the CLI, never by hand-editing the JSONL:
>
> ```powershell
> pwsh -NoProfile -File scripts/all_features/add.ps1 -Id "file-transfer.background-progress-hairline" -Area "File transfer" -Name "Background transfer progress on every screen" -Description "<see the call actually run>" -Flavors "standard,noLegal,lite,photos,legacy,vr" -Spec S1398
> ```
>
> (Corrected during execution: the parameter set written at planning time - `-Op` / `-Text` - does not exist. `add.ps1` takes `-Id -Area -Name -Description -Flavors` as required, plus optional `-Spec`. Check with `scripts/utils/help.ps1 -Name scripts/all_features/add.ps1` rather than assuming.)
>
> Then validate:
>
> ```powershell
> pwsh -NoProfile -File scripts/all_features/validate.ps1
> ```
>
> Do not touch `docs/FEATURES*.md` - the showcase is generated by `/skill-release` from the inventory diff.

**Why:**

Strategic §8 states the ticket introduces a user-visible capability and mandates an inventory record, and CLAUDE.md section 11 makes `docs/ALL_FEATURES.jsonl` the only per-spec destination for one.

**Verification:**

- `Grep` - `S1398` matches at least once in `docs/ALL_FEATURES.jsonl`.
- `validate.ps1` exits 0.
- `docs/FEATURES.md`, `_RU.md` and `_UK.md` are absent from this ticket's changed set.

**Status:** `[x] done`

**Step Log:**

- 2026-08-10 - Verification 3/3 PASS. Record `file-transfer.background-progress-hairline` added under area `File transfer`, flavors `standard,noLegal,lite,photos,legacy,vr` - all six, because the transfer machinery carries no flavor gate at all, so there is nothing to read a narrower list from. `validate.ps1`: PASS, 685 records. No `docs/FEATURES*.md` touched.

---

### Step 04.2 - Regenerate the class catalog

**Files:** `dev/CATALOG/app_v2.jsonl`, `dev/CATALOG/app_v2.md`
**Depends on:** Step 04.1

**Prompt for developer:**

> Run the catalog sync once for the whole ticket:
>
> ```powershell
> pwsh -NoProfile -File scripts/catalog_sync.ps1 -Module app_v2
> ```
>
> Then set `role` and `status` for the three new classes with `dev/CATALOG/scripts/set.ps1`: `BackgroundOperationBarView`, `BackgroundOperationTrackManager`, `BackgroundOperationBarAttachManager`. No `-NoFlavors` hint applies - all three live in `src/main` and ship in every flavor.

**Why:**

CLAUDE.md section 12 requires one catalog sync per ticket and requires new classes to carry `role` and `status`, without which the next ticket's catalog-first lookup cannot find them.

**Verification:**

- `dev/CATALOG/scripts/query.ps1 -ClassMatches "BackgroundOperation*"` returns four records.
- Each returned record has a non-empty `role`.

**Status:** `[x] done`

**Step Log:**

- 2026-08-10 - Verification 2/2 PASS. The plan said three classes; the catalog returns four, because `BackgroundOperationBarState` is its own file and is catalogued like any other. All four carry a role and `status=new`, read back from `dev/CATALOG/app_v2.jsonl` rather than from the setter's own success message. No `-NoFlavors` hint: every class sits in `src/main` and ships in all six flavors. Render: 2763 records.

---

### Step 04.3 - Close through the facade

**Files:** all files changed by this ticket
**Depends on:** Step 04.2

**Prompt for developer:**

> Close the whole ticket in one call, naming the entire changed set and scoping the gates to it:
>
> ```powershell
> pwsh -NoProfile -File scripts/post-change.ps1 -Files "<comma-separated full changed set>" -ScopeToFile -Target "S1398" -Description "S1398: hairline background-operation progress bar hosted by BaseActivity, Browse opted out" -ChangeType Mixed -Module app_v2
> ```
>
> Read the verdict line. `PASS` is the only clean outcome; `PASS WITH ADVISORIES (n)` requires reading each advisory. Exit 1 means a gate failed, exit 2 means it could not verify - the two are different answers and must not be reported alike.

**Why:**

CLAUDE.md section 12 makes the facade the mechanical closure path and requires the whole changed set be named, because naming one file while changing several certifies only that one file.

**Verification:**

- `post-change.ps1` prints `post-change: PASS` (or `PASS WITH ADVISORIES` with each advisory read) and exits 0.
- `dev/CHANGELOG.md` gains exactly one row for this ticket.

**Status:** `[x] done`

**Step Log:**

- 2026-08-10 - Verification 2/2 PASS. Closure ran per phase rather than as one call at the end, so each phase's gates judged that phase's own set. The final call returned `PASS WITH ADVISORIES (1)` naming `document-registry`; the advisory was read rather than waved through - `validate.ps1` reports PASS on 27 records and `generate.ps1 -Check` reports the generated views current, both exit 0, so nothing in this change is unregistered or stale. The gate's own wording says it could not attribute the finding to this change, and the two direct checks confirm it.

---

## Phase Done Criteria

- [x] Every `Step 04.*` above is `[x] done`.
- [x] Project compiles - no code changed in this phase; the proving build is Phase 03's `.\a.ps1 dq`, exit 0, which also packaged the APK.
- [x] Debug verification tags `Timber.d("S1398: ..")` present at three changed flow entries - the host decision in `BaseActivity`, the attach in `BackgroundOperationBarAttachManager`, and each state render in `BackgroundOperationBarView` - matching the `BlockNeedUserTest` status the ticket ends in. `ticket-log-audit` accepts them now that the status was flipped first.
- [ ] `/spec-check S1398` - deferred: it runs after the device test, and no device is attached.

---

## Handoff Notes to Next Phase

Final phase - see [INDEX.md](INDEX.md) Completion Gate. The `S1398:` debug tags stay in the source until the ticket leaves `BlockNeedUserTest`; whoever moves it to `Verified` deletes every one of them in the same change.

---

## Rollback Plan

Revert phase commit(s). The inventory record and the changelog row are append-only and are removed with the same revert; the catalog indexes are gitignored and regenerate from source.
