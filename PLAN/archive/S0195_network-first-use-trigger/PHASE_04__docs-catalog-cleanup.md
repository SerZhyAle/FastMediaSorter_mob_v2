# Phase 04 — docs-catalog-cleanup

**Strategic spec:** [`../S0195_network-first-use-trigger.md`](../S0195_network-first-use-trigger.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** ✅ Done
**Depends on:** Phase 03
**Blocks:** —
**Steps done:** 3 / 3
**Started:** 2026-05-14
**Completed:** 2026-05-14

---

## Objective

Close the S0195 implementation cycle: regenerate catalog, audit dev log entries, advance the ticket to `BlockNeedUserTest` so the device-test cycle starts. `docs/FEATURES.md` is not touched (strategic §8: "Без изменений").

---

## Prerequisites

- [ ] Phase 03 is ✅ Done — eager hooks removed, manual smoke verified.
- [ ] All §6 items resolved in INDEX.

---

## Files Touched

| File | New / Modified | Line budget |
|------|:--------------:|------------:|
| `dev/CATALOG/app_v2.jsonl` | Regenerated by script | n/a |
| `dev/CATALOG/app_v2.md` | Regenerated by script | n/a |
| `PLAN/S0195_network-first-use-trigger.md` | Modified (status) | n/a |

---

## Steps

### Step 04.1 — Regenerate catalog after new class

**Files:** `dev/CATALOG/app_v2.jsonl`, `dev/CATALOG/app_v2.md`
**Depends on:** Phase 03 done

**Prompt for developer:**

> Run `pwsh -File dev/CATALOG/scripts/scan.ps1 -Module app_v2` then `pwsh -File dev/CATALOG/scripts/render.ps1 -Module app_v2`. Confirm `NetworkLifecycleBootstrapper` is present in `dev/CATALOG/app_v2.jsonl`. Then run `pwsh -File dev/CATALOG/scripts/set.ps1 -Module app_v2 -ClassMatches "NetworkLifecycleBootstrapper" -Role "infra" -Status "tested"` to fill the manual fields.

**Verification:**

- `Grep` — `NetworkLifecycleBootstrapper` matches in `dev/CATALOG/app_v2.jsonl`.
- The JSONL record has `"role": "infra"` and `"status": "tested"` for that class.

**Status:** `[x] done`

**Step Log:**

- 2026-05-14 — Catalog scan: 1045 files. Set NetworkLifecycleBootstrapper role=infra status=tested. Rendered.

---

### Step 04.2 — Verify dev log completeness

**Files:** `dev/CHANGELOG.md` (read-only)
**Depends on:** Step 04.1

**Prompt for developer:**

> Confirm `dev/CHANGELOG.md` contains a `S0195` entry for every modified production file:
> - `NetworkLifecycleBootstrapper.kt` (Phase 01 — created)
> - `NetworkLifecycleModule.kt` (Phase 01 — KDoc only)
> - `SmbConnectionManager.kt` (Phase 02)
> - `SftpClient.kt` (Phase 02)
> - `FtpClient.kt` (Phase 02)
> - `GoogleDriveRestClient.kt` (Phase 02)
> - `DropboxClient.kt` (Phase 02)
> - `OneDriveRestClient.kt` (Phase 02)
> - `FastMediaSorterApp.kt` (Phase 03)
> - `PLAN/S0195_network-first-use-trigger.md` (status → BlockNeedUserTest)
>
> If any entry is missing, add it now via `./scripts/add_to_dev_log.ps1 "<path>" "S0195" "<description>"`.

**Verification:**

- `Grep -c "S0195"` in `dev/CHANGELOG.md` returns ≥ 10 hits.

**Status:** `[x] done`

**Step Log:**

- 2026-05-14 — Grep S0195 in dev/CHANGELOG.md: 19 hits (well above ≥10 threshold).

---

### Step 04.3 — Advance status and run `/spec-check S0195`

**Files:** `PLAN/spec-catalog.jsonl` (via update.ps1 only), `PLAN/S0195_network-first-use-trigger.md`
**Depends on:** Step 04.2

**Prompt for developer:**

> Advance status to `BlockNeedUserTest` via `pwsh -File scripts/spec_catalog/update.ps1 -Id S0195 -Status BlockNeedUserTest`. Update the `Status:` line in `PLAN/S0195_network-first-use-trigger.md` accordingly. The S0195 verification tag in `FastMediaSorterApp.kt` (added in Phase 03 Step 03.2) and `NetworkLifecycleBootstrapper.kt` (added in Phase 01 Step 01.1) must remain in code while status is `BlockNeedUserTest` — per CLAUDE.md invariant. They will be removed when the spec moves out of `BlockNeedUserTest` via `/spec-check` → `Verified`.

**Verification:**

- `pwsh -File scripts/spec_catalog/select.ps1 -Id S0195 -Format json | Select-String '"status"'` returns `"status": "BlockNeedUserTest"`.
- `Grep -c 'Timber.d("S0195:' app_v2/src/` returns 2 hits (one in `FastMediaSorterApp.kt`, one in `NetworkLifecycleBootstrapper.kt`).

**Status:** `[x] done`

**Step Log:**

- 2026-05-14 — Journal: S0195 In Progress -> BlockNeedUserTest. Strategic spec status updated. Timber.d("S0195:` tags: 2 hits (FastMediaSorterApp.onCreate + NetworkLifecycleBootstrapper SMB auto-reset notification). Tags remain until /spec-check returns Verified.

---

## Phase Done Criteria

- [ ] Every `Step 04.*` above is `[x] done`.
- [ ] `dev/CHANGELOG.md` has ≥ 10 S0195 entries.
- [ ] S0195 spec catalog status is `BlockNeedUserTest`.
- [ ] `dev/CATALOG/app_v2.jsonl` regenerated with the new class.
- [ ] `docs/FEATURES.md` — not touched (strategic §8 "Без изменений").

---

## Handoff Notes to Next Phase

Final phase — see INDEX.md Completion Gate. After device-test signs off, `/spec-check S0195` advances status to `Verified` and removes the two `Timber.d("S0195:` tags.

---

## Rollback Plan

Administrative phase — no code reverts. If the catalog regeneration produced incorrect output, re-run `scan.ps1` + `render.ps1`.
