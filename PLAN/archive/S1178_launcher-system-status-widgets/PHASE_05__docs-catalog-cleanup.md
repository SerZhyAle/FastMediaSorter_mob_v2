# Phase 05 - Docs, catalog and closure

**Strategic spec:** [`../S1178_launcher-system-status-widgets.md`](../S1178_launcher-system-status-widgets.md)
**Tactical index:** [`INDEX.md`](INDEX.md)
**Status:** ✅ Done
**Depends on:** Phase 01, Phase 02, Phase 03, Phase 04
**Blocks:** none - final phase
**Steps done:** 3 / 3
**Started:** 2026-08-08
**Completed:** 2026-08-08

---

## Objective

Record the shipped capability, make the new classes findable in the catalog, and place the device-test probes the ticket needs before it goes to the owner.

---

## Prerequisites

- [ ] All phases in "Depends on" are ✅ Done.
- [ ] Strategic §6 research items blocking this phase are Resolved.
- [ ] Working tree is clean or on a feature branch.

---

## Files Touched

| File | New / Modified | Line budget |
|------|:--------------:|------------:|
| `docs/ALL_FEATURES.jsonl` | Modified | +1 record |
| `dev/CATALOG/app_v2.jsonl` | Modified | regenerated |
| `app_v2/src/launcherEnabled/java/com/sza/fastmediasorter/ui/launcher/gadget/TechnicalGadget.kt` | Modified | ≤ 230 |

> `docs/FEATURES.md`, `docs/FEATURES_RU.md` and `docs/FEATURES_UK.md` are not touched - the public showcase is generated by `/skill-release` from the `ALL_FEATURES` diff (CLAUDE.md §11).
>
> `dev/CHANGELOG.md` is written by `scripts/post-change.ps1` and is never hand-edited.

---

## Steps

### Step 05.1 - Record the capability in the feature inventory

**Files:** `docs/ALL_FEATURES.jsonl`
**Depends on:** - start of phase

**Prompt for developer:**

> Add one record with `pwsh -NoProfile -File scripts/all_features/add.ps1`, in English, describing the four technical desktop gadgets - network and connection, battery with remaining-time estimate, free space, memory and uptime - as placeable on the launcher home screen. Declare the flavors from the gate, not from memory: the capability ships where the launcher surface is mounted. Then run `pwsh -NoProfile -File scripts/all_features/validate.ps1`.

**Why:**

Strategic §8 states this is a new feature with a showcase sentence, and CLAUDE.md §11 makes `ALL_FEATURES.jsonl` the record a spec writes so `/skill-release` can generate the public showcase from the diff.

**Verification:**

- `Grep` - the new record present in `docs/ALL_FEATURES.jsonl`.
- `pwsh -NoProfile -File scripts/all_features/validate.ps1` exits 0.
- `Grep` - `docs/FEATURES.md` unchanged by this ticket.

**Status:** `[x]` done

**Step Log:**

- 2026-08-08 - Verification 3/3 PASS. Record `launcher.technical-status-gadgets` added via `all_features/add.ps1`;
  `all_features/validate.ps1` PASS, 673 records. `docs/FEATURES*.md` untouched - `/skill-release` owns it.
- Flavors declared from the gate, not from memory: `app_v2/build.gradle.kts` adds `src/launcherEnabled/java`
  inside the `standard` and `noLegal` blocks only, so the record reads `standard,noLegal`.

---

### Step 05.2 - Make the new classes findable in the catalog

**Files:** `dev/CATALOG/app_v2.jsonl`
**Depends on:** Step 05.1

**Prompt for developer:**

> Run `pwsh -NoProfile -File scripts/catalog_sync.ps1 -Module app_v2` once for the whole ticket. Then set `role` and `status` for every class this ticket added with `pwsh -NoProfile -File dev/CATALOG/scripts/set.ps1 -Module app_v2 -Path "<class path>"`, and on the same call declare the launcher-only classes absent elsewhere with `-NoFlavors "lite,photos,legacy,vr"` - `TechnicalGadget`, `TechnicalGadgetFormatter` and `TechnicalGadgetModule` live in a source set those four flavors never mount.

**Why:**

Strategic §11.10 requires the gadget set to be absent from `lite`, `photos` and `legacy`; source-set placement makes that true, and the catalog hint makes it searchable instead of implicit.

**Verification:**

- `pwsh -NoProfile -File dev/CATALOG/scripts/query.ps1 -ClassMatches "TechnicalGadget*"` returns the three classes with a non-empty `role`.
- `Grep` - `noFlavors` present on the `TechnicalGadget` record in `dev/CATALOG/app_v2.jsonl`.

**Status:** `[x]` done

**Step Log:**

- 2026-08-08 - Verification 2/2 PASS. `query.ps1 -ClassMatches "TechnicalGadget*"` returns six records
  (three files, two declarations each) all carrying a non-empty `role` and `status=new`; `noFlavors` =
  `[lite,photos,legacy,vr]` on all six. Roles also set on `GetNetworkStatusUseCase` and `NetworkStatus`.
- `catalog_sync.ps1` was re-run afterwards by `post-change.ps1` and the roles survived - the scan preserves
  the hand-set fields rather than overwriting them.

---

### Step 05.3 - Place the device-test probes and close the ticket mechanically

**Files:** `app_v2/src/launcherEnabled/java/com/sza/fastmediasorter/ui/launcher/gadget/TechnicalGadget.kt`
**Depends on:** Step 05.2

**Prompt for developer:**

> Add one `Timber.d("S1178: technical gadget %s active", key)` at the entry of `TechnicalGadgetView.onActive` - one probe for the whole ticket at the single flow entry all four gadgets pass through, not one per phase and not one per gadget. Then run `pwsh -NoProfile -File scripts/post-change.ps1 -Files "<every file this ticket changed>" -Target "S1178" -Description "Launcher technical status gadgets" -ChangeType Mixed -Module app_v2 -ScopeToFile` and read its verdict: only the bare word `PASS` is a clean close. The probe stays until the ticket leaves `BlockNeedUserTest`.

**Why:**

CLAUDE.md §2 makes a `Timber.d("S1178: ..")` probe mandatory while the ticket sits in `BlockNeedUserTest`, and strategic §11 lists ten criteria that only a device can confirm - the probe is what ties the owner's observation to this ticket.

**Verification:**

- `Grep` - `Timber.d("S1178:` matches exactly once across `app_v2/src/**/*.kt`.
- `scripts/post-change.ps1` exits 0 and prints `post-change: PASS`.
- `Grep` - `S1178` returns zero hits in any `Timber.i`, `Timber.w` or `Timber.e` call.

**Status:** `[x]` done

**Step Log:**

- 2026-08-08 - Verification 3/3 PASS. `Timber.d("S1178:` matches exactly once across `app_v2/src/**/*.kt`,
  at the entry of `TechnicalGadgetView.onActive` - the one flow entry all four gadgets pass through.
  `S1178` in `Timber.i`/`w`/`e` = 0. `post-change.ps1 -ScopeToFile` exit 0, `post-change: PASS`.
- The probe prints the gadget key (`network`/`battery`/`storage`/`resources`), so the owner's log names which
  cell woke up; the view took a `key` parameter for exactly this.
- Status was flipped to `BlockNeedUserTest` BEFORE the gate ran - `assert-no-ticket-logs` only tolerates a
  probe whose ticket is already in that status.

---

## Phase Done Criteria

- [x] Every `Step 05.*` above is `[x] done`.
- [x] Project compiles - `fk` 38s and `fkn` 21s after the final probe edit, both BUILD SUCCESSFUL (2026-08-08).
- [x] `Grep` for `TODO(phase-05)` returns zero hits.
- [x] Dev log entry written by `scripts/post-change.ps1` for the ticket.
- [x] `dev/CATALOG/app_v2.jsonl` regenerated.
- [x] Phase-boundary audit run - see Audit below.

---

## Phase-boundary audit (2026-08-08)

- **Probe invariant.** Exactly one `Timber.d("S1178:` line exists and the ticket is in `BlockNeedUserTest`;
  the two halves of CLAUDE.md §2 agree. It is deleted when the ticket leaves that status, not before.
- **Inventory vs showcase.** The capability is recorded in `docs/ALL_FEATURES.jsonl` only. `docs/FEATURES*.md`
  is `/skill-release`-owned and was not touched (CLAUDE.md §11).
- **Registry acknowledgement.** `docs/ALL_FEATURES.jsonl` is a registered document; its sibling
  `docs/ALL_FEATURES.schema.json` needed no edit - the record adds no field, and `Launcher` is an area 31 other
  records already use.
- **P0/P1 open:** none.

---

## Handoff Notes to Next Phase

Final phase - see INDEX.md Completion Gate. The probe added in step 05.3 is removed by `/spec-check` when the ticket reaches `Verified`.

---

## Rollback Plan

Revert phase commit(s) - documentation, catalog and one probe line, no product behaviour changed.
