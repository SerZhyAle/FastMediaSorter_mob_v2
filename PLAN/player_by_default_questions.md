# Опросник: Уточнение требований (Default Player)

*Пожалуйста, заполните ответы в квадратных скобках `[ ]` или текстом ниже вопроса.*

---

### **Вопрос 1: Механика кнопок в Настройках**
Android 10+ **запрещает** приложениям тихо делать себя "по умолчанию" для медиа файлов. Мы можем только открыть системный экран или диалог выбора (`Chooser`).
**Какой вариант UX выберем для кнопок в настройках?**
- [ ] **Вариант А**: Симулировать открытие тестового `.mp3` / `.mp4` файла, чтобы Android показал системный диалог *"Открыть с помощью (Всегда)"*.
- [Х] **Вариант Б**: Открывать системный экран "Приложения по умолчанию" (или О приложении) и выводить диалог-подсказку с инструкцией для пользователя.
- [ ] **Вариант В**: Ваш комментарий: _____________________________________

---

### **Вопрос 2: Список расширений для категории "Документы"**
Вы упомянули: `EPUB`, `PDF`, `TXT`.
**Нужно ли добавлять поддержку других форматов в Intent-клики?**
- [ ] `FB2`
- [ ] `RTF`
- [ ] `HTML / MHT`
- [ ] Ограничиться строго указанными (EPUB, PDF, TXT).

Ограничится теми форматами, которые мы поддерживаем точно. Т.е. EPUB, PDF, TXT.
---

### **Вопрос 3: Фильтры и Сборки (Flavors)**
В проекте есть сборки `lite` (без аудио/облаков) и `photos` (только фото).
**Должны ли Intent-фильтры отключаться в зависимости от сборки?**
*(Если да, то в версии `photos` пользователи не увидят приложение в списке для `.mp3`)*
- [X] **Да**, отключать фильтры программно через `PackageManager` при первом старте (более чистый подход).
- [ ] **Нет**, пусть будут общие фильтры на все файлы, а внутри приложения покажем ошибку "Не поддерживается в этой версии".

---

### **Вопрос 4: Перехват кнопок магнитолы (Авто)**
Для запуска плеера кнопкой с руля/магнитолы (Play/Pause) недостаточно быть "программой для файлов". Нужно иметь активную службу на фоне.
**Нужно ли в рамках этой задачи реализовывать `MediaButtonReceiver`?**
*(Это позволит плееру запускаться с кнопки даже из убитого состояния).*
- [X] **Да**, нужен старт службы по Bluetooth/Медиа кнопкам.
- [ ] **Нет**, достаточно обычной работы программы в фоне/когда она открыта.

Но эта функция должна работать только если стоит галочка play on background. То есть, если пользователь ее отключает, значит он готов их к таким серьезным изменениям. То есть теперь это для него будет плеер для перехвата в силу системных команд. Я понимаю, что это немножко разные Задачи, но мы их объединим в одну галочку.
---

### **Вопрос 5: Архитектура Временного ресурса**
Я предлагаю концепцию **In-Memory** (в памяти): вы открываете файл -> создается виртуальный ресурс -> после выхода из плеера он удаляется. Это не забивает базу данных.
**Вы согласны с таким подходом?**
- [X] **Да**, в памяти (clean).
- [ ] **Нет**, создавать скрытую запись в БД и чистить ее потом.
- [ ] Комментарий: _____________________________________

Я понимаю, что можем показать файл просто в памяти без ресурса. У меня немножко другое, что если он захочет этот файл, листнуть на следующий. Или поставить на него в избранный. То есть Эмм, когда он этот файл опять сможет найти. Что у нас еще есть? У нас есть еще возможность работать с предопределенным ресурсом недавние. То есть мы можем файл, который мы получаем в качестве параметра для открытия, добавить в ресурс недавнее. прям последним и когда пользователь выйдет из этого файла он окажется в этом ресурсе . Ну, хотя тут уже, конечно, есть вопрос, потому что ресурс недавний пользователь может или не создавать, или удалить, и у него его может не быть. Надо бы размышлять об этом.