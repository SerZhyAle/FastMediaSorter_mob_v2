{
  "$comment": "Overlay B (Android). The `release/` tag prefix is a second clock on the same version and must stay in editionTagPrefixes or the version check misreads the newest tag. Feature source of truth is docs/ALL_FEATURES.jsonl, written through its CLI - never hand-edited.",

  "canon": {
    "version": "2026.09.22.2",
    "coreDigest": "sha256:13abcb8a7c2f0a354328bdd03c12e75ea361b6f8208134dc1ba3d1bd884e567d",
    "adoptedOn": "2026-09-23",
    "model": "reference",
    "contribRecord": "contrib/fastmediasorter_mob_v2.md"
  },

  "role": "product",

  "overlay": ["B"],
  "shapes": ["flavor"],
  "editions": [],

  "versionShape": {
    "tagRegex": "^v\\d+\\.\\d+\\.\\d+\\.\\d+$",
    "editionTagPrefixes": ["release/"]
  },

  "ledgerShape": 3,
  "ledgerFile": "docs/ALL_FEATURES.jsonl",

  "channels": ["github", "play"],

  "site": {
    "kind": "product",
    "root": ".",
    "pages": ["index.html", "index-ru.html", "index-uk.html"]
  },

  "privacy": {
    "page": "docs/PRIVACY_POLICY.md",
    "carveOut": null,
    "sensitiveAccess": false
  },

  "byteIdenticalPairs": [],
  "vendorAllow": [],
  "exemptions": [
    {
      "id": "SZA-SEC04",
      "path": "scripts/quality.tests/AssertNoSecrets.Tests.ps1",
      "reason": "Contract suite of the repo's own secret gate: it seeds each detector with a synthetic private-key header. Same file: entry in scripts/quality/no-secrets-allowlist.txt."
    },
    {
      "id": "SZA-SEC04",
      "path": "wear/src/test/resources/golden/wear-payload/payload_sync_v1.json",
      "reason": "Golden v1 sync payload: the sshPrivateKey field is a stub whose base64 body decodes to the word openssh. Same file: entry in scripts/quality/no-secrets-allowlist.txt."
    }
  ]
}
