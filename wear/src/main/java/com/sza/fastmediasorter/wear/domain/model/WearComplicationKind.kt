package com.sza.fastmediasorter.wear.domain.model

/**
 * S2047: enumeration of supported Wear OS complication data sources.
 * Mirrors WearTileKind for structural consistency across Wear OS surfaces.
 */
enum class WearComplicationKind {
    LAST_RESOURCE,
    FAVOURITES_COUNT,
    NOW_PLAYING
}
