<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <profileable android:shell="true" />

        <receiver
            android:name=".perf.BenchmarkSetupReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.sza.fastmediasorter.perf.action.PREPARE_APP" />
            </intent-filter>
        </receiver>

        <activity
            android:name=".perf.BenchmarkRouteActivity"
            android:exported="true"
            android:excludeFromRecents="true"
            android:noHistory="true"
            android:theme="@style/Theme.FastMediaSorter.NoActionBar">
            <intent-filter>
                <action android:name="com.sza.fastmediasorter.perf.action.OPEN_JOURNEY" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>
    </application>

</manifest>
