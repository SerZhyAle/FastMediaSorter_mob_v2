package com.sza.fastmediasorter.domain.model.tourist

/**
 * S2922: available telemetry and navigation tiles on the Tourist dashboard.
 */
enum class TouristTileType {
    SPEED,
    ALTITUDE,
    COMPASS,
    COORDINATES,
    SATELLITES,
    STEPS,
    TRIP_DISTANCE,
    SUN_TIME,
}
