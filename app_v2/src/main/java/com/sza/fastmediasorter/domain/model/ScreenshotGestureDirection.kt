package com.sza.fastmediasorter.domain.model

/** Directional identity of a screenshot edge gesture, threaded from the overlay manager to the dispatcher. */
enum class ScreenshotGestureDirection {
    DOWN,
    RIGHT,
    UP
}
