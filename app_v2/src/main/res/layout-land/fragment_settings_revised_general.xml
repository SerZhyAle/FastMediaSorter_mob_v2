<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Landscape keeps the same direct row ownership as portrait so revised ordering and hidden
         legacy-only rows stay identical across orientations. -->
    <include
        android:id="@+id/revisedGeneralLegacyContent"
        layout="@layout/fragment_settings_general" />

</FrameLayout>