<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- Landscape keeps the same direct row ownership as portrait so scheduled/destinations
         regrouping stays identical for keyboard, D-pad, mouse, and touch tests. -->
    <include
        android:id="@+id/revisedOperationsLegacyContent"
        layout="@layout/fragment_settings_destinations" />

</FrameLayout>