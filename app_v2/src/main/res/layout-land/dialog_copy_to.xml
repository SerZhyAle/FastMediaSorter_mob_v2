<?xml version="1.0" encoding="utf-8"?>
<!-- Landscape: layoutDestinations wrapped in NestedScrollView; btnCancel always visible below -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/padding_large"
    android:background="@drawable/bg_copy_dialog">

    <TextView
        android:id="@+id/tvFileCount"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.Material3.BodyMedium"
        android:textAlignment="center"
        android:paddingBottom="@dimen/margin_small" />

    <com.google.android.material.progressindicator.LinearProgressIndicator
        android:id="@+id/progressBar"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:contentDescription="@string/operation_progress_indicator"
        android:indeterminate="true"
        android:visibility="gone"
        app:indicatorColor="?attr/colorPrimary" />

    <!-- Scrollable destination buttons -->
    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:maxHeight="@dimen/dialog_landscape_list_max_height"
        android:layout_marginTop="@dimen/margin_large">

        <LinearLayout
            android:id="@+id/layoutDestinations"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:orientation="vertical" />

    </androidx.core.widget.NestedScrollView>

    <!-- Select folder button - shown when no destinations are configured or to pick a custom path -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnSelectFolder"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_small"
        android:contentDescription="@string/select_folder"
        android:text="@string/select_folder"
        style="@style/Widget.FastMediaSorter.Button.DialogConfirm" />

    <!-- Cancel always visible below scroll -->
    <com.google.android.material.button.MaterialButton
        android:id="@+id/btnCancel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:layout_marginTop="@dimen/margin_large"
        android:text="@android:string/cancel"
        style="@style/Widget.FastMediaSorter.Button.DialogCancel" />

</LinearLayout>
