<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Core actions -->
    <item
        android:id="@+id/menu_delete"
        android:icon="@drawable/ic_delete"
        android:title="@string/delete"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_favorite"
        android:icon="@drawable/ic_star_outline"
        android:title="@string/favorite"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_share"
        android:icon="@drawable/ic_share"
        android:title="@string/share"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_info"
        android:icon="@drawable/ic_info"
        android:title="@string/file_information"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_fullscreen"
        android:icon="@drawable/ic_fullscreen"
        android:title="@string/fullscreen_mode"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_slideshow"
        android:icon="@drawable/ic_play"
        android:title="@string/slideshow"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_random"
        android:icon="@drawable/ic_random_nav"
        android:title="@string/random_file_description"
        app:showAsAction="never" />

    <!-- S0050: Black Screen mode (AUDIO/VIDEO only) -->
    <item
        android:id="@+id/menu_black_screen"
        android:icon="@drawable/ic_black_screen"
        android:title="@string/black_screen_button_title"
        app:showAsAction="never" />

    <!-- Print (PDF/TXT/IMAGE) -->
    <item
        android:id="@+id/menu_print"
        android:icon="@drawable/ic_print"
        android:title="@string/menu_print"
        app:showAsAction="never" />

    <!-- Save Frame (VIDEO only) — saves current frame as PNG to configured destination -->
    <item
        android:id="@+id/menu_save_frame"
        android:icon="@drawable/ic_save_frame"
        android:title="@string/menu_save_frame"
        app:showAsAction="never" />

    <!-- 3DVR toggle (VR flavor only) — relaunch current video in immersive VR mode -->
    <item
        android:id="@+id/menu_3d_vr"
        android:icon="@drawable/ic_vr_3d"
        android:title="@string/vr_toggle_enter_description"
        app:showAsAction="never" />

    <!-- Search (PDF/TXT/EPUB) -->
    <item
        android:id="@+id/menu_search"
        android:icon="@drawable/ic_search"
        android:title="@string/search"
        app:showAsAction="never" />



    <!-- Google Lens (PDF) -->
    <item
        android:id="@+id/menu_google_lens"
        android:icon="@drawable/ic_google_lens"
        android:title="@string/google_lens"
        app:showAsAction="never" />

    <!-- OCR (PDF) -->
    <item
        android:id="@+id/menu_ocr"
        android:icon="@drawable/ic_ocr"
        android:title="@string/ocr_button_description"
        app:showAsAction="never" />

    <!-- Translate (PDF/TXT/EPUB) -->
    <item
        android:id="@+id/menu_translate"
        android:icon="@drawable/ic_translate"
        android:title="@string/translate"
        app:showAsAction="never" />

    <!-- Text Settings (Translation & OCR Settings) -->
    <item
        android:id="@+id/menu_text_settings"
        android:icon="@drawable/ic_book"
        android:title="@string/translation_settings"
        app:showAsAction="never" />



    <!-- Lyrics (Audio files) -->
    <item
        android:id="@+id/menu_lyrics"
        android:icon="@drawable/ic_microphone"
        android:title="@string/lyrics"
        app:showAsAction="never" />

    <!-- Search in YouTube Music (Audio files) -->
    <item
        android:id="@+id/menu_search_youtube_music"
        android:icon="@drawable/ic_youtube_music"
        android:title="@string/search_in_youtube_music"
        app:showAsAction="never" />

    <!-- Cast to Chromecast (IMAGE/GIF/AUDIO/VIDEO, Wi-Fi only) -->
    <item
        android:id="@+id/menu_cast"
        android:icon="@drawable/ic_cast"
        android:title="@string/cast_to_chromecast"
        app:showAsAction="never" />

    <!-- Rename -->
    <item
        android:id="@+id/menu_rename"
        android:icon="@drawable/ic_rename"
        android:title="@string/rename"
        app:showAsAction="never" />

    <!-- Copy Text (TXT) -->
    <item
        android:id="@+id/menu_copy_text"
        android:icon="@drawable/ic_copy"
        android:title="@string/copy"
        app:showAsAction="never" />



    <!-- Edit (Image/Video/TXT) -->
    <item
        android:id="@+id/menu_edit"
        android:icon="@drawable/ic_edit_20"
        android:title="@string/edit"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_edit_text"
        android:icon="@drawable/ic_edit_20"
        android:title="@string/edit"
        app:showAsAction="never" />



    <!-- Undo (if available) -->
    <item
        android:id="@+id/menu_undo"
        android:icon="@drawable/ic_undo"
        android:title="@string/undo"
        app:showAsAction="never" />

    <!-- Sleep Timer (Audio) -->
    <item
        android:id="@+id/menu_sleep_timer"
        android:icon="@drawable/ic_sleep_timer"
        android:title="@string/menu_sleep_timer"
        app:showAsAction="never" />

    <!-- Re-open with Encoding (TXT) -->
    <item
        android:id="@+id/menu_reopen_encoding"
        android:icon="@drawable/ic_refresh"
        android:title="@string/reopen_with_encoding"
        app:showAsAction="never" />

    <!-- Toggle Markdown rendering (TXT .md only) -->
    <item
        android:id="@+id/menu_toggle_markdown"
        android:icon="@drawable/ic_view_list"
        android:title="@string/toggle_markdown"
        app:showAsAction="never" />

    <!-- Reader Settings (TXT) -->
    <item
        android:id="@+id/menu_reader_settings"
        android:icon="@drawable/ic_settings"
        android:title="@string/reader_settings"
        app:showAsAction="never" />

    <!-- Read Aloud TTS (TXT) -->
    <item
        android:id="@+id/menu_read_aloud"
        android:icon="@drawable/ic_microphone"
        android:title="@string/read_aloud"
        app:showAsAction="never" />

    <!-- PDF Scroll Mode Toggle -->
    <item
        android:id="@+id/menu_pdf_scroll_mode"
        android:icon="@drawable/ic_view_list"
        android:title="@string/pdf_scroll_mode"
        app:showAsAction="never" />

    <!-- PDF Color Mode (Night/Sepia) -->
    <item
        android:id="@+id/menu_pdf_color_mode"
        android:icon="@drawable/ic_night_mode"
        android:title="@string/pdf_night_mode"
        app:showAsAction="never" />

    <!-- PDF Thumbnail Navigation -->
    <item
        android:id="@+id/menu_pdf_thumbnails"
        android:icon="@drawable/ic_view_list"
        android:title="@string/pdf_thumbnails"
        app:showAsAction="never" />

    <!-- EPUB Reader Settings -->
    <item
        android:id="@+id/menu_epub_reader_settings"
        android:icon="@drawable/ic_settings"
        android:title="@string/epub_reader_settings"
        app:showAsAction="never" />

    <!-- EPUB Cross-Chapter Search -->
    <item
        android:id="@+id/menu_epub_search_all"
        android:icon="@drawable/ic_search"
        android:title="@string/epub_search_all_chapters"
        app:showAsAction="never" />

</menu>
