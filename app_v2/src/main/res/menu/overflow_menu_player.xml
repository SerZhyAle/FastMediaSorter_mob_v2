<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <!-- Search (PDF/TXT/EPUB) -->
    <item
        android:id="@+id/menu_search"
        android:icon="@android:drawable/ic_menu_search"
        android:title="@string/search"
        app:showAsAction="never" />



    <!-- Google Lens (PDF) -->
    <item
        android:id="@+id/menu_google_lens"
        android:icon="@drawable/ic_google_lens"
        android:title="@string/google_lens"
        app:showAsAction="never" />

    <!-- OCR (PDF) -->
    <item
        android:id="@+id/menu_ocr"
        android:icon="@drawable/ic_ocr"
        android:title="@string/ocr_button_description"
        app:showAsAction="never" />

    <!-- Translate (PDF/TXT/EPUB) -->
    <item
        android:id="@+id/menu_translate"
        android:icon="@drawable/ic_translate"
        android:title="@string/translate"
        app:showAsAction="never" />

    <!-- Text Settings (Translation & OCR Settings) -->
    <item
        android:id="@+id/menu_text_settings"
        android:icon="@drawable/ic_book"
        android:title="@string/translation_settings"
        app:showAsAction="never" />



    <!-- Lyrics (Audio files) -->
    <item
        android:id="@+id/menu_lyrics"
        android:icon="@drawable/ic_microphone"
        android:title="@string/lyrics"
        app:showAsAction="never" />

    <!-- Rename -->
    <item
        android:id="@+id/menu_rename"
        android:icon="@drawable/ic_rename"
        android:title="@string/rename"
        app:showAsAction="never" />

    <!-- Copy Text (TXT) -->
    <item
        android:id="@+id/menu_copy_text"
        android:icon="@android:drawable/ic_menu_save"
        android:title="@string/copy"
        app:showAsAction="never" />



    <!-- Edit (Image/Video/TXT) -->
    <item
        android:id="@+id/menu_edit"
        android:icon="@android:drawable/ic_menu_edit"
        android:title="@string/edit"
        app:showAsAction="never" />

    <item
        android:id="@+id/menu_edit_text"
        android:icon="@android:drawable/ic_menu_edit"
        android:title="@string/edit"
        app:showAsAction="never" />



    <!-- Undo (if available) -->
    <item
        android:id="@+id/menu_undo"
        android:icon="@android:drawable/ic_menu_revert"
        android:title="@string/undo"
        app:showAsAction="never" />

    <!-- Sleep Timer (Audio) -->
    <item
        android:id="@+id/menu_sleep_timer"
        android:icon="@drawable/ic_sleep_timer"
        android:title="@string/menu_sleep_timer"
        app:showAsAction="never" />

    <!-- Re-open with Encoding (TXT) -->
    <item
        android:id="@+id/menu_reopen_encoding"
        android:icon="@android:drawable/ic_menu_sort_alphabetically"
        android:title="@string/reopen_with_encoding"
        app:showAsAction="never" />

    <!-- Toggle Markdown rendering (TXT .md only) -->
    <item
        android:id="@+id/menu_toggle_markdown"
        android:icon="@android:drawable/ic_menu_view"
        android:title="@string/toggle_markdown"
        app:showAsAction="never" />

    <!-- Reader Settings (TXT) -->
    <item
        android:id="@+id/menu_reader_settings"
        android:icon="@android:drawable/ic_menu_preferences"
        android:title="@string/reader_settings"
        app:showAsAction="never" />

    <!-- Read Aloud TTS (TXT) -->
    <item
        android:id="@+id/menu_read_aloud"
        android:icon="@android:drawable/ic_lock_silent_mode_off"
        android:title="@string/read_aloud"
        app:showAsAction="never" />

    <!-- PDF Scroll Mode Toggle -->
    <item
        android:id="@+id/menu_pdf_scroll_mode"
        android:icon="@drawable/ic_view_list"
        android:title="@string/pdf_scroll_mode"
        app:showAsAction="never" />

    <!-- PDF Color Mode (Night/Sepia) -->
    <item
        android:id="@+id/menu_pdf_color_mode"
        android:icon="@drawable/ic_night_mode"
        android:title="@string/pdf_night_mode"
        app:showAsAction="never" />

    <!-- PDF Thumbnail Navigation -->
    <item
        android:id="@+id/menu_pdf_thumbnails"
        android:icon="@drawable/ic_view_list"
        android:title="@string/pdf_thumbnails"
        app:showAsAction="never" />

    <!-- EPUB Reader Settings -->
    <item
        android:id="@+id/menu_epub_reader_settings"
        android:icon="@drawable/ic_settings"
        android:title="@string/epub_reader_settings"
        app:showAsAction="never" />

</menu>
