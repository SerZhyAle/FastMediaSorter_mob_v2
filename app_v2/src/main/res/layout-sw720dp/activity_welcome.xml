<?xml version="1.0" encoding="utf-8"?>
<!-- Activity shell for large screens / Android TV (sw >= 720dp).
     Buttons are larger and positioned for comfortable D-pad navigation.
     Navigation is in a horizontal top bar like the sw480dp layout, but
     with larger touch targets and text sized for TV viewing distance. -->
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="?attr/colorSurface">

    <!-- Top Navigation Bar -->
    <LinearLayout
        android:id="@+id/layoutTopNav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:padding="@dimen/welcome_top_nav_padding"
        android:gravity="center_vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent">

        <!-- Previous Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnPrevious"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/previous"
            android:textColor="?attr/colorOnSurface"
            android:alpha="0.7"
            android:textSize="@dimen/welcome_button_text_size"
            android:padding="@dimen/welcome_button_padding"
            android:minHeight="@dimen/welcome_button_min_height"
            android:minWidth="0dp"
            android:visibility="gone" />

        <Space
            android:layout_width="0dp"
            android:layout_height="1dp"
            android:layout_weight="1" />

        <!-- Next Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnNext"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/next"
            android:textSize="@dimen/welcome_button_text_size"
            android:textColor="@color/white"
            android:padding="@dimen/welcome_button_padding"
            android:minHeight="@dimen/welcome_button_min_height"
            android:minWidth="0dp"
            app:backgroundTint="?attr/colorPrimary" />

        <!-- Finish Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnFinish"
            style="@style/Widget.MaterialComponents.Button"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/finish"
            android:textSize="@dimen/welcome_button_text_size"
            android:textColor="@color/white"
            android:padding="@dimen/welcome_button_padding"
            android:minHeight="@dimen/welcome_button_min_height"
            android:minWidth="0dp"
            app:backgroundTint="?attr/colorPrimary"
            android:visibility="gone" />

        <!-- Skip Button -->
        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnSkip"
            style="@style/Widget.MaterialComponents.Button.TextButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:text="@string/skip"
            android:textColor="?attr/colorOnSurface"
            android:alpha="0.7"
            android:textSize="@dimen/welcome_button_text_size"
            android:padding="@dimen/welcome_button_padding"
            android:minHeight="@dimen/welcome_button_min_height"
            android:minWidth="0dp" />

    </LinearLayout>

    <!-- ViewPager2 for Welcome Pages -->
    <androidx.viewpager2.widget.ViewPager2
        android:id="@+id/viewPager"
        android:layout_width="0dp"
        android:layout_height="0dp"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        app:layout_constraintTop_toBottomOf="@id/layoutTopNav"
        app:layout_constraintBottom_toTopOf="@id/layoutIndicator"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- Page Indicator - centred at bottom -->
    <LinearLayout
        android:id="@+id/layoutIndicator"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:orientation="horizontal"
        android:layout_marginBottom="@dimen/welcome_indicator_margin_bottom"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
