<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- The revised host reuses the proven General row tree directly so regrouping happens here
         without hosting GeneralSettingsFragment; settings_category_about remains inside the
         included content and legacy-only rows are hidden by RevisedGeneralSectionBinder. -->
    <include
        android:id="@+id/revisedGeneralLegacyContent"
        layout="@layout/fragment_settings_general" />

</FrameLayout>