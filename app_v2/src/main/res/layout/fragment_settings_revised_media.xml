<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <!-- settings_category_images and the other mapped Media groups stay in the hosted legacy
         Media content until row-level revised UI lands in a later phase. -->
    <FrameLayout
        android:id="@+id/revisedMediaContentContainer"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

</FrameLayout>