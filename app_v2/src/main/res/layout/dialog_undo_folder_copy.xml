<?xml version="1.0" encoding="utf-8"?>
<!-- S1326: the copy-undo confirmation. States what the delete removes and what it leaves alone rather
     than asking "are you sure" (COMMUNICATION_POLICY 2.7), and carries the named destructive/cancel
     pair CLAUDE.md 11 fixes - MaterialAlertDialogBuilder's own buttons cannot take those styles.
     Orientation-independent: wrap_content and centred, the same shape
     dialog_network_monitor_clear_history.xml ships; the landscape variant (S3254) carries the same
     focus chain. -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/dialog_padding_large">

    <TextView
        android:id="@+id/tvDialogTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/margin_large"
        android:text="@string/undo_folder_copy_confirm_title"
        android:textAlignment="center"
        android:textAppearance="@style/TextAppearance.Material3.TitleLarge" />

    <TextView
        android:id="@+id/tvMessage"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="@dimen/dialog_padding_large"
        android:text="@string/undo_folder_copy_confirm_message"
        android:textAlignment="center"
        android:textAppearance="@style/TextAppearance.Material3.BodyLarge" />

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="horizontal">

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnCancel"
            style="@style/Widget.FastMediaSorter.Button.DialogCancel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:clickable="true"
            android:focusable="true"
            android:nextFocusRight="@+id/btnDeleteCopies"
            android:text="@string/cancel" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/btnDeleteCopies"
            style="@style/Widget.FastMediaSorter.Button.DialogDestructive"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="@dimen/dialog_action_button_gap"
            android:clickable="true"
            android:focusable="true"
            android:nextFocusLeft="@+id/btnCancel"
            android:text="@string/undo_folder_copy_confirm_action" />
    </LinearLayout>
</LinearLayout>
