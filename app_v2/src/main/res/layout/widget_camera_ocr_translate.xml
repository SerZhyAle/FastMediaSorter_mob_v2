<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/widget_camera_ocr_translate_container"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@drawable/widget_background"
    android:gravity="center"
    android:orientation="vertical"
    android:padding="@dimen/padding_tiny">

    <ImageView
        android:id="@+id/widget_camera_ocr_translate_icon"
        android:layout_width="@dimen/item_icon_size_small"
        android:layout_height="@dimen/item_icon_size_small"
        android:contentDescription="@string/setting_camera_ocr_translation_title"
        android:src="@drawable/ic_camera_ocr_translate"
        app:tint="@color/white" />

    <TextView
        android:id="@+id/widget_camera_ocr_translate_label"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/margin_tiny"
        android:ellipsize="end"
        android:gravity="center"
        android:maxLines="1"
        android:text="@string/widget_camera_ocr_translate_label"
        android:textColor="@color/white"
        android:textSize="@dimen/item_detail_text_size_small"
        android:textStyle="bold" />

</LinearLayout>
