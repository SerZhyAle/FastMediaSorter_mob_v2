<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/padding_normal">

    <!-- S0947: optional in-layout title for card-style hosts (migrated quick-launch pickers); the
         standalone dialog uses the MaterialAlertDialog title instead and leaves this gone. -->
    <TextView
        android:id="@+id/tvOptionPickerTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_small"
        android:textAppearance="?attr/textAppearanceTitleMedium"
        android:visibility="gone" />

    <!-- S0947: hidden by default; shown only when the option list overflows its viewport (measured at
         runtime, orientation-correct). -->
    <com.google.android.material.textfield.TextInputLayout
        android:id="@+id/layoutOptionSearch"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_small"
        android:hint="@string/streams_picker_search_hint"
        android:visibility="gone"
        app:boxBackgroundMode="outline"
        app:endIconMode="clear_text">

        <com.google.android.material.textfield.TextInputEditText
            android:id="@+id/editOptionSearch"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:imeOptions="actionSearch|flagNoExtractUi"
            android:inputType="text"
            android:singleLine="true" />

    </com.google.android.material.textfield.TextInputLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerOptions"
        android:layout_width="match_parent"
        android:layout_height="@dimen/dialog_list_max_height"
        android:minHeight="@dimen/button_height"
        android:overScrollMode="ifContentScrolls"
        android:scrollbars="vertical" />

    <TextView
        android:id="@+id/tvOptionsEmpty"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:padding="@dimen/padding_normal"
        android:text="@string/streams_picker_empty"
        android:textColor="@color/text_color_secondary"
        android:visibility="gone" />

</LinearLayout>
