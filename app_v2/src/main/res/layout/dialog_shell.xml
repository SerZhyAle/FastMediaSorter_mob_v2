<?xml version="1.0" encoding="utf-8"?>
<!-- S3242: reusable dialog shell - title, scrolling content frame, action pair pinned outside the
     scroll region so buttons stay visible on short screens (docs/ui/PHONE_UI_COMPONENT_PATTERNS.md §5.4). -->
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/dialog_padding_large">

    <TextView
        android:id="@+id/tvDialogTitle"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:textAppearance="@style/TextAppearance.FastMediaSorter.Dialog.Title"
        android:paddingBottom="@dimen/margin_large" />

    <androidx.core.widget.NestedScrollView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="@dimen/margin_large">

        <FrameLayout
            android:id="@+id/dialogContentFrame"
            android:layout_width="match_parent"
            android:layout_height="wrap_content" />

    </androidx.core.widget.NestedScrollView>

    <include layout="@layout/dialog_action_pair" />

</LinearLayout>
