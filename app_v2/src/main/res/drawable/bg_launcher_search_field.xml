<?xml version="1.0" encoding="utf-8"?>
<!-- S1566: pill background for the launcher search field. Theme-attr colour only, unlike the older
     bg_rounded_input, which is white-on-transparent for the text viewer's dark overlay and would be
     unreadable on a light-theme desktop cell. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <corners android:radius="24dp" />
    <solid android:color="?attr/colorSurfaceVariant" />
    <stroke
        android:width="1dp"
        android:color="?attr/colorOutline" />
</shape>
