<?xml version="1.0" encoding="utf-8"?>
<!-- S1286 (generalized from S1095): opaque rounded surface behind every self-drawn searchable picker
     so its title and search field read clearly over the darkened host screen. Theme-attr solid (valid
     from API 21; minSdk is 26). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurface" />
    <corners android:radius="16dp" />
</shape>
