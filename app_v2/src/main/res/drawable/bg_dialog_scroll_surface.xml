<?xml version="1.0" encoding="utf-8"?>
<!-- Distinct surface for the scrollable text area: reuses the Material3 colorSurfaceVariant theme
     attr (S0378 strategic §6 item 3) so the text panel reads apart from the dialog background in
     both light and dark themes. Non-color cue (always-visible scrollbar) is set on the ScrollView. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurfaceVariant" />
    <corners android:radius="8dp" />
</shape>
