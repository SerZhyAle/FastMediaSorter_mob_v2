<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Storage permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_VIDEO" />
    <uses-permission android:name="android.permission.READ_MEDIA_AUDIO" />

    <!-- All Files Access for file management (Android 11+) -->
    <!-- Required to select system folders like Pictures, Downloads, DCIM via folder picker -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ScopedStorage" />

    <!-- MANAGE_MEDIA permission for direct file deletion without dialogs (Android 12+) -->
    <uses-permission android:name="android.permission.MANAGE_MEDIA" />
    <uses-permission android:name="android.permission.CAMERA" />


    <!-- Network permissions -->
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_LOCAL_NETWORK" android:minSdkVersion="37" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <!-- Wi-Fi is used for network sources but not required for local files or cellular cloud access -->
    <uses-feature android:name="android.hardware.wifi" android:required="false" />
    <!-- Keyboard/mouse/remote navigation is already supported; touchscreen is not mandatory -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <!-- DPAD/trackball-only devices (streaming sticks, some Android boxes) have no faketouch layer -->
    <uses-feature android:name="android.hardware.faketouch" android:required="false" />
    <!-- TV set-top boxes appear in Play Store without forcing leanback mode -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <!-- RECORD_AUDIO implicitly requires hardware.microphone on Google Play; opt out so TVs / mic-less devices stay eligible -->
    <uses-feature android:name="android.hardware.microphone" android:required="false" />
    <!-- required=false keeps Play Store availability for camera-less devices such as XR headsets. -->
    <uses-feature android:name="android.hardware.camera" android:required="false" />
    <uses-feature android:name="android.hardware.camera.any" android:required="false" />

    <!-- Keep screen on. tools:remove undoes the android:maxSdkVersion="25" contributed
         by the Chucker debug library (com.github.chuckerteam.chucker:library:4.0.0).
         Without this remove, the merged manifest caps WAKE_LOCK at API 25 and
         WorkManager.startForeground crashes on API 26+ trying to acquire a WakeLock
         (SecurityException: "Neither user … nor current process has WAKE_LOCK"). -->
    <uses-permission android:name="android.permission.WAKE_LOCK" tools:remove="android:maxSdkVersion" />

    <!-- Haptic feedback on Wear sync success (BeamAnimationDialog) -->
    <uses-permission android:name="android.permission.VIBRATE" />

    <!-- Audio background playback service -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Microphone recording (S0100). Implicit hardware.microphone requirement is overridden via <uses-feature> above. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />

    <!-- Scheduled file operations -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- S0349: microphone foreground service type for the Quick Audio Recorder widget (API 34+) -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MICROPHONE" />
    <uses-permission android:name="android.permission.REQUEST_IGNORE_BATTERY_OPTIMIZATIONS" />

    <!-- Screen sizes and densities supported -->
    <!-- Supports phones, tablets, and compact devices (480x480+) -->
    <supports-screens android:smallScreens="true" android:normalScreens="true" android:largeScreens="true" android:xlargeScreens="true" android:anyDensity="true" android:requiresSmallestWidthDp="320" />

    <!-- Package visibility: allow the system to resolve CREATE_DOCUMENT (SAF file picker) on API 30+ -->
    <!-- Also declare camera intents so queryIntentActivities() can find camera apps on API 30+ -->
    <queries>
        <intent>
            <action android:name="android.intent.action.CREATE_DOCUMENT" />
        </intent>
        <intent>
            <action android:name="android.media.action.IMAGE_CAPTURE" />
        </intent>
        <intent>
            <action android:name="android.media.action.VIDEO_CAPTURE" />
        </intent>
        <!-- S0200: Chrome Custom Tabs package resolution on API 30+ -->
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/msword" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/rtf" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:mimeType="application/vnd.oasis.opendocument.text" />
        </intent>
        <intent>
            <action android:name="android.support.customtabs.action.CustomTabsService" />
        </intent>
        <!-- S0362: Google Keep package visibility (Android 11+). Without this the editor
             "Send to Keep" availability check and the draw export cannot resolve Keep and
             the menu item silently disappears / falls back to the generic chooser. -->
        <package android:name="com.google.android.keep" />
        <package android:name="com.google.android.keep.notes" />
        <!-- S0459: visibility for the unified «Send to..» package-backed receivers on API 30+, so the
             availability resolver (getPackageInfo) and setPackage targeting resolve installed clients;
             without these a receiver silently falls back to the generic chooser. -->
        <package android:name="org.telegram.messenger" />
        <package android:name="org.telegram.messenger.web" />
        <package android:name="org.thunderdog.challegram" />
        <package android:name="org.telegram.plus" />
        <package android:name="org.telegram.messenger.beta" />
        <package android:name="com.whatsapp" />
        <package android:name="com.whatsapp.w4b" />
        <package android:name="com.instagram.android" />
        <!-- S0459: Google Lens probes these packages (GoogleLensShare); without visibility on API 30+
             resolveActivity() returns null even when installed and Lens always falls back to chooser. -->
        <package android:name="com.google.ar.lens" />
        <package android:name="com.google.android.googlequicksearchbox" />
        <!-- Email clients resolve via the mailto scheme so the email receiver can bias toward them. -->
        <intent>
            <action android:name="android.intent.action.SENDTO" />
            <data android:scheme="mailto" />
        </intent>
    </queries>

    <application android:name=".FastMediaSorterApp" android:allowBackup="true" android:dataExtractionRules="@xml/data_extraction_rules" android:fullBackupContent="@xml/backup_rules" android:banner="@drawable/tv_banner" android:icon="@mipmap/ic_launcher" android:label="@string/app_name" android:largeHeap="true" android:roundIcon="@mipmap/ic_launcher_round" android:supportsRtl="true" android:theme="@style/Theme.FastMediaSorter" android:networkSecurityConfig="@xml/network_security_config" android:localeConfig="@xml/locales_config" android:enableOnBackInvokedCallback="true" tools:targetApi="34">

        <!-- Google Cast SDK: auto-discover Chromecast devices on the LAN -->
        <meta-data android:name="com.google.android.gms.cast.framework.OPTIONS_PROVIDER_CLASS_NAME" android:value="com.sza.fastmediasorter.core.cast.CastOptionsProvider" />

        <!-- Disable automatic WorkManager initialization -->
        <provider android:name="androidx.startup.InitializationProvider" android:authorities="${applicationId}.androidx-startup" android:exported="false" tools:node="merge">
            <meta-data android:name="androidx.work.WorkManagerInitializer" android:value="androidx.startup" tools:node="remove" />
        </provider>

        <activity android:name=".ui.main.MainActivity" android:exported="true" android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <!--
              <layout> provides the initial large landscape window size when the app is launched
              on a VR headset (Quest) in panel mode for any product flavor (standard/lite/photos/legacy).
              HorizonOS uses defaultWidth/defaultHeight as the first-launch default; subsequent
              launches restore the last user-adjusted size natively. Has no visible effect on
              phones/tablets in non-freeform mode.
            -->
            <layout android:defaultWidth="1920dp" android:defaultHeight="1080dp" android:gravity="center" android:minWidth="400dp" android:minHeight="300dp" />
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <!-- TV launcher: makes the app appear in Google TV / Android TV app grid -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
                <!-- Categorize app as system APP_FILES manager -->
                <category android:name="android.intent.category.APP_FILES" />
            </intent-filter>
            <meta-data android:name="android.app.shortcuts" android:resource="@xml/shortcuts" />
        </activity>

        <activity android:name=".ui.browse.BrowseActivity" android:exported="true" android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity android:name=".ui.duplicates.DuplicatesActivity" android:exported="false" android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity android:name=".ui.game.GameActivity" android:exported="false" android:label="@string/game_title" android:configChanges="keyboardHidden" android:parentActivityName=".ui.main.MainActivity" />

        <activity android:name=".ui.game.GameHelpActivity" android:exported="false" android:label="@string/game_help_title" android:configChanges="orientation|screenSize|screenLayout|keyboardHidden" android:parentActivityName=".ui.game.GameActivity" />

        <activity android:name=".ui.player.PlayerActivity" android:exported="true" android:supportsPictureInPicture="true" android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden" android:theme="@style/Theme.FastMediaSorter.FullScreen" />

        <activity android:name=".ui.settings.SettingsActivity" android:exported="true" android:label="@string/settings" android:configChanges="orientation|screenSize|keyboardHidden" android:parentActivityName=".ui.main.MainActivity" android:windowSoftInputMode="adjustPan" />

        <activity android:name=".ui.calculator.CalculatorActivity" android:exported="false" android:label="@string/calculator_title" android:configChanges="orientation|screenSize|keyboardHidden" android:parentActivityName=".ui.main.MainActivity" />

        <activity android:name=".ui.cameraocr.CameraOcrTranslateActivity" android:exported="false" android:label="@string/setting_camera_ocr_translation_title" android:configChanges="orientation|screenSize|keyboardHidden" android:parentActivityName=".ui.main.MainActivity" android:theme="@style/Theme.FastMediaSorter" />
        <activity android:name=".ui.cameracapture.CameraCaptureActivity" android:exported="false" android:configChanges="orientation|screenSize|keyboardHidden" android:theme="@style/Theme.FastMediaSorter" />

        <activity android:name=".ui.keybinding.KeybindingRemapActivity" android:exported="false" android:configChanges="orientation|screenSize|keyboardHidden" android:parentActivityName=".ui.settings.SettingsActivity" />

        <!-- S0116 §5.1 pillar K: saved auth sessions sub-screen launched from PlaybackSettingsFragment. -->
        <activity android:name=".ui.settings.auth.AuthSessionsActivity" android:exported="false" android:label="@string/setting_saved_authorizations_title" android:configChanges="orientation|screenSize|keyboardHidden" android:parentActivityName=".ui.settings.SettingsActivity" />

        <!-- S0473 Phase 04: opt-in statistics dashboard launched from General settings (visible only when the toggle is on). -->
        <activity android:name=".ui.statistics.StatisticsActivity" android:exported="false" android:label="@string/statistics_title" android:configChanges="orientation|screenSize|keyboardHidden" android:parentActivityName=".ui.settings.SettingsActivity" />

        <activity android:name=".ui.addresource.AddResourceActivity" android:exported="false" android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity android:name=".ui.resourceeditor.ResourceEditorActivity" android:exported="false" android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity android:name=".ui.welcome.WelcomeActivity" android:exported="false" android:configChanges="orientation|screenSize|keyboardHidden" android:theme="@style/Theme.FastMediaSorter.NoActionBar" />

        <activity android:name=".ui.cloudfolders.GoogleDriveFolderPickerActivity" android:exported="true" android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity android:name=".ui.cloudfolders.DropboxFolderPickerActivity" android:exported="true" android:configChanges="orientation|screenSize|keyboardHidden" />

        <activity android:name=".ui.cloudfolders.OneDriveFolderPickerActivity" android:exported="true" android:configChanges="orientation|screenSize|keyboardHidden" />

        <!-- AppAuth redirect receiver for Google Drive browser OAuth on Quest / XR without GMS. -->
        <activity android:name="net.openid.appauth.RedirectUriReceiverActivity" android:exported="true" tools:node="replace">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <!-- Custom-scheme AppAuth redirect: com.sza.fastmediasorter:/oauth2redirect has no
                     host by design, so AppLinkUrlError's "host required" check does not apply. -->
                <data android:scheme="com.sza.fastmediasorter" android:path="/oauth2redirect" tools:ignore="AppLinkUrlError" />
            </intent-filter>
        </activity>

        <!-- Dedicated completion hop keeps AppAuth redirect parsing out of the host activities. -->
        <activity android:name=".ui.cloudauth.GoogleDriveAuthCompletionActivity" android:exported="false" android:excludeFromRecents="true" android:noHistory="true" android:theme="@style/Theme.FastMediaSorter.NoActionBar" />

        <!-- Dropbox Auth Activity -->
        <activity android:name="com.dropbox.core.android.AuthActivity" android:configChanges="orientation|keyboard" android:exported="true" android:launchMode="singleTask">
            <intent-filter>
                <data android:scheme="db-${dropboxAppKey}" />
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.BROWSABLE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- MSAL Browser Tab Activity for OneDrive. All cloud-enabled flavors reuse the
             standard msauth host (com.sza.fastmediasorter), so every signing-hash that may
             produce an installable cloud-enabled build MUST be listed below as a separate
             <data android:path="…"/> entry. Origin: S0232.

             Adding a new signing keystore requires BOTH steps - neither alone is enough:
               (a) add a new <intent-filter> + <data android:path="<hash>"/> here, AND
               (b) register the matching redirect URI in the Azure App, Google Cloud OAuth
                   console (S0233), and Dropbox app console (S0235).
             Forgetting (a) -> MsalClientException "Intent filter for BrowserTabActivity is
             missing"; forgetting (b) -> provider rejects the redirect at the OAuth step. -->
        <activity android:name="com.microsoft.identity.client.BrowserTabActivity" android:exported="true" android:configChanges="orientation|keyboardHidden|screenSize|smallestScreenSize|screenLayout" tools:replace="android:configChanges">
            <!-- Release keystore signature hash -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="msauth" android:host="com.sza.fastmediasorter" android:path="/FYsxzaNPAAPFK3rigkV29z+r0es=" />
            </intent-filter>
            <!-- Debug keystore signature hash -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="msauth" android:host="com.sza.fastmediasorter" android:path="/WdRIvjP3wXJ5jte7TPUOqtT59es=" />
            </intent-filter>
            <!-- Custom debug keystore signature hash (debug.keystore.properties -> debugCustom) -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="msauth" android:host="com.sza.fastmediasorter" android:path="/iRMe/7fhUe3Plj8y2z5NIOOXsZ8=" />
            </intent-filter>
            <!-- Release keystore (fastmediasorter.keystore) signature hash -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="msauth" android:host="com.sza.fastmediasorter" android:path="/rk9B49kRMWq5OZ+1ZF76MAavAIg=" />
            </intent-filter>
        </activity>

        <!-- Widget Configuration Activities -->
        <activity android:name=".widget.ResourceLaunchWidgetConfigActivity" android:exported="true" android:theme="@style/Theme.FastMediaSorter">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <activity android:name=".widget.RandomPhotoFrameConfigActivity" android:exported="true" android:theme="@style/Theme.FastMediaSorter">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Favorites Widget -->
        <receiver android:name=".widget.FavoritesWidgetProvider" android:exported="true" android:label="@string/widget_favorites_label" android:icon="@drawable/ic_widget_favorites">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_favorites_info" />
        </receiver>

        <!-- Favorites Widget Service -->
        <service android:name=".widget.FavoritesWidgetService" android:permission="android.permission.BIND_REMOTEVIEWS" android:exported="false" />

        <!-- Scheduled Tasks Widget (S0353) -->
        <receiver android:name=".widget.ScheduledTasksWidgetProvider" android:exported="true" android:label="@string/widget_scheduled_tasks_label" android:icon="@drawable/ic_widget_scheduled_tasks">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.sza.fastmediasorter.action.SCHEDULED_RUN_ALL" />
                <action android:name="com.sza.fastmediasorter.action.SCHEDULED_TOGGLE_PAUSE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_scheduled_tasks_info" />
        </receiver>
        <service android:name=".widget.ScheduledTasksWidgetService" android:permission="android.permission.BIND_REMOTEVIEWS" android:exported="false" />

        <!-- Audio background playback service (Media3 MediaSessionService) -->
        <service android:name=".ui.player.AudioPlaybackService" android:foregroundServiceType="mediaPlayback" android:exported="true">
            <intent-filter>
                <action android:name="androidx.media3.session.MediaSessionService" />
            </intent-filter>
        </service>

        <!-- Quick Settings tile for audio play/pause toggle -->
        <service android:name=".core.AudioToggleTileService" android:icon="@drawable/ic_tile_play" android:label="@string/tile_audio_label" android:permission="android.permission.BIND_QUICK_SETTINGS_TILE" android:exported="true">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE_PREFERENCES" />
            </intent-filter>
            <meta-data android:name="android.service.quicksettings.ACTIVE_TILE" android:value="false" />
        </service>

        <!-- Wearable Data Layer - receives sync requests and acks from the watch companion -->
        <service android:name=".service.PhoneWearListenerService" android:exported="true">
            <intent-filter>
                <action android:name="com.google.android.gms.wearable.DATA_CHANGED" />
                <action android:name="com.google.android.gms.wearable.MESSAGE_RECEIVED" />
                <data android:host="*" android:pathPrefix="/fms/network_sources" android:scheme="wear" />
            </intent-filter>
        </service>

        <!-- Resource Launch Widget -->
        <receiver android:name=".widget.ResourceLaunchWidgetProvider" android:exported="true" android:label="@string/widget_resource_launch_label" android:icon="@drawable/ic_widget_resource_launch">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_resource_launch_info" />
        </receiver>

        <!-- Continue Reading Widget -->
        <receiver android:name=".widget.ContinueReadingWidgetProvider" android:exported="true" android:label="@string/widget_continue_reading_label" android:icon="@drawable/ic_widget_continue_reading">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_continue_reading_info" />
        </receiver>

        <!-- Camera Photos Widget (S0134) -->
        <receiver android:name=".widget.CameraPhotosWidgetProvider" android:exported="true" android:label="@string/widget_camera_photos_label" android:icon="@drawable/ic_widget_camera_photos">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_camera_photos_info" />
        </receiver>

        <!-- Random Photo Frame Widget -->
        <receiver android:name=".widget.RandomPhotoFrameWidgetProvider" android:exported="true" android:label="@string/widget_random_photo_frame_label" android:icon="@drawable/ic_widget_camera_photos">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_random_photo_frame_info" />
        </receiver>

        <!-- Random Music Widget (S0134) - removed in photos flavor (no audio support) -->
        <receiver android:name=".widget.RandomMusicWidgetProvider" android:exported="true" android:label="@string/widget_random_music_label" android:icon="@drawable/ic_widget_random_music">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_random_music_info" />
        </receiver>

        <!-- Audio Now Playing Widget -->
        <receiver android:name=".widget.AudioNowPlayingWidgetProvider" android:exported="true" android:label="@string/widget_audio_now_playing_label" android:icon="@drawable/ic_widget_random_music">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
                <action android:name="com.sza.fastmediasorter.action.AUDIO_WIDGET_TOGGLE_FAVORITE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_audio_now_playing_info" />
        </receiver>

        <!-- Quick Audio Recorder Widget (S0349) - removed in lite/photos (SUPPORT_MIC_RECORDING=false) -->
        <receiver android:name=".widget.QuickAudioRecorderWidgetProvider" android:exported="true" android:label="@string/widget_quick_audio_recorder_label" android:icon="@drawable/ic_widget_quick_audio_recorder">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_quick_audio_recorder_info" />
        </receiver>

        <!-- Quick Audio Recorder: transparent trampoline that toggles recording from a widget tap (S0349) -->
        <activity android:name=".widget.QuickAudioRecorderActivity" android:exported="false" android:theme="@style/Theme.FastMediaSorter.Transparent" android:excludeFromRecents="true" android:taskAffinity="" android:noHistory="true" />

        <!-- Quick Audio Recorder: microphone foreground service that owns MediaRecorder (S0349) -->
        <service android:name=".widget.QuickAudioRecorderService" android:exported="false" android:foregroundServiceType="microphone" />

        <!-- Quick Camera Capture Widget (S0369): captures a photo and saves it into a configured
             target. Removed where camera capture is not meaningful (flavor manifest gating). -->
        <receiver android:name=".widget.CameraQuickCaptureWidgetProvider" android:exported="true" android:label="@string/widget_camera_quick_capture_label" android:icon="@drawable/ic_widget_camera_quick_capture">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_camera_quick_capture_info" />
        </receiver>

        <!-- Quick Camera Capture: transparent trampoline that hosts the capture flow from a widget tap (S0369) -->
        <activity android:name=".widget.CameraQuickCaptureActivity" android:exported="false" android:theme="@style/Theme.FastMediaSorter.Transparent" android:excludeFromRecents="true" android:taskAffinity="" android:noHistory="true" />

        <!-- Camera Launch Widget (S0568): config-less, opens the unified in-app camera (photo/video)
             and saves captures to the device public folders. Present wherever the in-app camera is
             meaningful; runtime degradation handled by the trampoline (no flavor manifest gating). -->
        <receiver android:name=".widget.CameraLaunchWidgetProvider" android:exported="true" android:label="@string/widget_camera_launch_label" android:icon="@drawable/ic_widget_camera_launch_accent">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/camera_launch_widget_info" />
        </receiver>

        <!-- Camera Launch: transparent trampoline that opens the unified camera from a widget tap (S0568) -->
        <activity android:name=".widget.CameraLaunchActivity" android:exported="false" android:theme="@style/Theme.FastMediaSorter.Transparent" android:excludeFromRecents="true" android:taskAffinity="" android:noHistory="true" />

        <!-- Quick Camera Capture: per-widget target configuration activity (S0369) -->
        <activity android:name=".widget.CameraQuickCaptureConfigActivity" android:exported="true">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_CONFIGURE" />
            </intent-filter>
        </activity>

        <!-- Calculator Widget -->
        <receiver android:name=".widget.CalculatorWidgetProvider" android:exported="true" android:label="@string/widget_calculator_label" android:icon="@drawable/ic_widget_calculator">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_calculator_info" />
        </receiver>

        <!-- Camera OCR Translate Widget -->
        <receiver android:name=".widget.CameraOcrTranslateWidgetProvider" android:exported="true" android:label="@string/widget_camera_ocr_translate_label" android:icon="@drawable/ic_camera_ocr_translate">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_camera_ocr_translate_info" />
        </receiver>

        <!-- Capture & OCR Panel Widget -->
        <receiver android:name=".widget.CaptureOcrPanelWidgetProvider" android:exported="true" android:label="@string/widget_capture_ocr_panel_label" android:icon="@drawable/ic_camera_ocr_translate">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_capture_ocr_panel_info" />
        </receiver>

        <!-- Embedded Game Widget -->
        <receiver android:name=".widget.GameLaunchWidgetProvider" android:exported="true" android:label="@string/game_widget_label" android:icon="@drawable/ic_game_kryvavitsa">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data android:name="android.appwidget.provider" android:resource="@xml/widget_game_launch_info" />
        </receiver>

        <!-- Scheduled file operations: foreground service type for WorkManager (Android 14+) -->
        <service android:name="androidx.work.impl.foreground.SystemForegroundService" android:foregroundServiceType="mediaPlayback|dataSync" tools:node="merge" />

        <!-- Scheduled file operations: reschedule on device boot -->
        <receiver android:name=".worker.ScheduledOperationsBootReceiver" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
            </intent-filter>
        </receiver>

        <!-- Media button receiver for cold service restart (enabled only when isPrimaryMediaPlayer=true) -->
        <receiver android:name=".ui.player.MediaButtonRestartReceiver" android:enabled="false" android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MEDIA_BUTTON" />
            </intent-filter>
        </receiver>

        <!-- FileProvider for sharing files -->
        <provider android:name="androidx.core.content.FileProvider" android:authorities="${applicationId}.fileprovider" android:exported="false" android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_provider_paths" />
        </provider>

        <!-- Standalone Player for external intents. Retained as a fallback / direct-launch target;
             external routing now goes through the per-type specialized activities + aliases below (S0380). -->
        <activity android:name=".ui.player.StandalonePlayerActivity" android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden" android:exported="true" android:supportsPictureInPicture="true" android:taskAffinity="${applicationId}.player" android:theme="@style/Theme.FastMediaSorter.FullScreen" />

        <!-- S0380: specialized standalone activities. Reached via the per-type VIEW aliases below
             (runtime-toggled) and the dispatcher trampoline. exported=false - the aliases own external
             access; the dispatcher forwards explicitly within the app. Routing is in the aliases. -->
        <activity android:name=".ui.player.standalone.PhotoVideoStandaloneActivity" android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden" android:exported="false" android:supportsPictureInPicture="true" android:taskAffinity="${applicationId}.player" android:theme="@style/Theme.FastMediaSorter.FullScreen" />
        <activity android:name=".ui.player.standalone.AudioStandaloneActivity" android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden" android:exported="false" android:theme="@style/Theme.FastMediaSorter.FullScreen" />
        <activity android:name=".ui.player.standalone.DocumentStandaloneActivity" android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden" android:exported="false" android:theme="@style/Theme.FastMediaSorter.FullScreen" />
        <activity android:name=".ui.player.standalone.TextStandaloneActivity" android:configChanges="orientation|screenSize|screenLayout|smallestScreenSize|keyboardHidden" android:exported="false" android:theme="@style/Theme.FastMediaSorter.FullScreen" />

        <!-- S0380: no-UI dispatcher trampoline for typeless / generic-wildcard VIEW intents. Reached via
             the .StandaloneTypelessPlayer alias; resolves the real media family and forwards explicitly
             to the matching specialized activity without loading heavy media SDKs. -->
        <activity android:name=".ui.player.dispatch.StandalonePlayerDispatcherActivity" android:exported="false" android:theme="@style/Theme.FastMediaSorter.Transparent" />

        <!-- Share Receiver: handles ACTION_SEND intents from the system Share sheet.
             Enabled/disabled dynamically via DefaultPlayerManager.applyShareReceiverState().
             Translucent theme ensures only the Copy-to dialog is visible (no blank window). -->
        <activity android:name=".ui.share.ReceiveShareActivity" android:exported="false" android:configChanges="orientation|screenSize|keyboardHidden" android:theme="@style/Theme.FastMediaSorter.Transparent" />

        <!-- S0422: resource share-file receiver. VIEW (open with) + SEND (share sheet). The vendor
             MIME is what our own export sets, giving a reliable friend-to-friend SEND path; the
             file pathPattern is a best-effort VIEW match for file managers using file:// URIs.
             application/octet-stream is intentionally not registered (chooser-noise; see
             PLAN/S0422_resource-import-file-format/research/01__file-association.md). -->
        <activity android:name=".ui.resourceimport.ResourceImportActivity" android:exported="true" android:excludeFromRecents="true" android:configChanges="orientation|screenSize|keyboardHidden" android:theme="@style/Theme.FastMediaSorter.Transparent">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/vnd.fms.resources+xml" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.fmsr" />
                <data android:mimeType="*/*" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/vnd.fms.resources+xml" />
            </intent-filter>
        </activity>

        <!-- Phase 2: Activity aliases for file-type associations (disabled by default) -->

        <!-- Audio alias: mp3, flac, aac, ogg, m4a, wma, opus, amr, etc. -->
        <activity-alias android:name=".StandaloneAudioPlayer" android:enabled="false" android:exported="true" android:targetActivity=".ui.player.standalone.AudioStandaloneActivity">
            <!-- Open audio files from file managers / share sheets -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="audio/mpeg" />
                <data android:mimeType="audio/flac" />
                <data android:mimeType="audio/aac" />
                <data android:mimeType="audio/ogg" />
                <data android:mimeType="audio/mp4" />
                <data android:mimeType="audio/x-ms-wma" />
                <data android:mimeType="audio/opus" />
                <data android:mimeType="audio/3gpp" />
                <data android:mimeType="audio/midi" />
                <data android:mimeType="audio/x-matroska" />
                <data android:mimeType="audio/*" />
            </intent-filter>
            <!-- Required for RoleManager.ROLE_MUSIC eligibility (Android 10+) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.APP_MUSIC" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity-alias>

        <!-- Video alias: mp4, mkv, avi, mov, webm, flv, wmv, 3gp, etc. -->
        <activity-alias android:name=".StandaloneVideoPlayer" android:enabled="false" android:exported="true" android:taskAffinity="${applicationId}.player" android:targetActivity=".ui.player.standalone.PhotoVideoStandaloneActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="video/mp4" />
                <data android:mimeType="video/x-matroska" />
                <data android:mimeType="video/x-msvideo" />
                <data android:mimeType="video/quicktime" />
                <data android:mimeType="video/x-ms-wmv" />
                <data android:mimeType="video/x-flv" />
                <data android:mimeType="video/webm" />
                <data android:mimeType="video/3gpp" />
                <data android:mimeType="video/mpeg" />
                <data android:mimeType="video/mp2t" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity-alias>

        <!-- Image alias: jpg, png, webp, heic, heif, bmp, avif (NO gif) -->
        <activity-alias android:name=".StandaloneImagePlayer" android:enabled="false" android:exported="true" android:taskAffinity="${applicationId}.player" android:targetActivity=".ui.player.standalone.PhotoVideoStandaloneActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="image/jpeg" />
                <data android:mimeType="image/png" />
                <data android:mimeType="image/webp" />
                <data android:mimeType="image/heic" />
                <data android:mimeType="image/heif" />
                <data android:mimeType="image/bmp" />
                <data android:mimeType="image/avif" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity-alias>

        <!-- Documents alias: pdf, epub, Office handoff. Plain text routes to .StandaloneTextPlayer below (S0380). -->
        <activity-alias android:name=".StandaloneDocsPlayer" android:enabled="false" android:exported="true" android:targetActivity=".ui.player.standalone.DocumentStandaloneActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:mimeType="application/pdf" />
                <data android:mimeType="application/epub+zip" />
                <data android:mimeType="application/msword" />
                <data android:mimeType="application/vnd.openxmlformats-officedocument.wordprocessingml.document" />
                <data android:mimeType="application/rtf" />
                <data android:mimeType="text/rtf" />
                <data android:mimeType="application/vnd.oasis.opendocument.text" />
            </intent-filter>
        </activity-alias>

        <!-- Text alias: plain text -> the lightest standalone surface (S0380). Split out of the docs alias
             so text never loads the document/PDF/WebView stack. -->
        <activity-alias android:name=".StandaloneTextPlayer" android:enabled="false" android:exported="true" android:targetActivity=".ui.player.standalone.TextStandaloneActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
                <!-- text/plain only: a broad text/* would collide with the docs alias's text/rtf
                     (RTF is a document) and surface a duplicate chooser entry (S0380 Step 05.3). -->
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity-alias>

        <!-- Dispatcher alias: typeless VIEW intents (S0380 Step 05.3). The scheme-only filter (a URI
             scheme with NO mimeType) matches only intents that carry no type - which the per-type aliases,
             each requiring a declared mimeType, never match. So the dispatcher uniquely owns the typeless
             case with zero chooser overlap, then forwards to the matching specialized activity.
             A `* / *` mimeType filter is deliberately omitted: it would match every concrete typed intent
             (image/jpeg, video/mp4, ..) and surface a duplicate "Open with" entry next to each per-type
             alias - the exact duplication this phase must avoid. Generic `* / *` intents still reach the
             app through the per-type aliases' family wildcards (image/*, video/*, audio/*). -->
        <activity-alias android:name=".StandaloneTypelessPlayer" android:enabled="false" android:exported="true" android:targetActivity=".ui.player.dispatch.StandalonePlayerDispatcherActivity">
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" />
                <data android:scheme="file" />
            </intent-filter>
        </activity-alias>

        <!-- ACTION_SEND aliases: appear in the system Share sheet (disabled by default).
             Enabled/disabled at runtime via DefaultPlayerManager.applyShareReceiverState().
             URI arrives as EXTRA_STREAM, so no scheme filter is needed here. -->

        <!-- Audio share receiver -->
        <activity-alias android:name=".StandaloneAudioSender" android:enabled="false" android:exported="true" android:targetActivity=".ui.share.ReceiveShareActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="audio/mpeg" />
                <data android:mimeType="audio/flac" />
                <data android:mimeType="audio/aac" />
                <data android:mimeType="audio/ogg" />
                <data android:mimeType="audio/mp4" />
                <data android:mimeType="audio/x-ms-wma" />
                <data android:mimeType="audio/opus" />
                <data android:mimeType="audio/3gpp" />
                <data android:mimeType="audio/midi" />
                <data android:mimeType="audio/x-matroska" />
                <data android:mimeType="audio/*" />
            </intent-filter>
        </activity-alias>

        <!-- Video share receiver -->
        <activity-alias android:name=".StandaloneVideoSender" android:enabled="false" android:exported="true" android:targetActivity=".ui.share.ReceiveShareActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="video/mp4" />
                <data android:mimeType="video/x-matroska" />
                <data android:mimeType="video/x-msvideo" />
                <data android:mimeType="video/quicktime" />
                <data android:mimeType="video/x-ms-wmv" />
                <data android:mimeType="video/x-flv" />
                <data android:mimeType="video/webm" />
                <data android:mimeType="video/3gpp" />
                <data android:mimeType="video/mpeg" />
                <data android:mimeType="video/mp2t" />
                <data android:mimeType="video/*" />
            </intent-filter>
        </activity-alias>

        <!-- Image share receiver -->
        <activity-alias android:name=".StandaloneImageSender" android:enabled="false" android:exported="true" android:targetActivity=".ui.share.ReceiveShareActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="image/jpeg" />
                <data android:mimeType="image/png" />
                <data android:mimeType="image/webp" />
                <data android:mimeType="image/heic" />
                <data android:mimeType="image/heif" />
                <data android:mimeType="image/bmp" />
                <data android:mimeType="image/avif" />
                <data android:mimeType="image/*" />
            </intent-filter>
        </activity-alias>

        <!-- Text/Link share receiver: handles plain-text from Instagram, browsers, etc.
             Creates a .txt file in temp cache, then shows the Copy-to dialog. -->
        <activity-alias android:name=".StandaloneTextSender" android:enabled="false" android:exported="true" android:targetActivity=".ui.share.ReceiveShareActivity">
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
                <data android:mimeType="text/*" />
            </intent-filter>
        </activity-alias>

        <!-- Remove ML Kit's Firebase DataTransport JobScheduler service to prevent ANR
             on devices with outdated Google Play Services. DataTransport falls back to
             AlarmManagerSchedulerBroadcastReceiver which does not block the main thread. -->
        <service android:name="com.google.android.datatransport.runtime.scheduling.jobscheduling.JobInfoSchedulerService" tools:node="remove" />

    </application>

</manifest>
