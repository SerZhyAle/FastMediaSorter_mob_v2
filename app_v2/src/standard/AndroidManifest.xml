<?xml version="1.0" encoding="utf-8"?>
<!--
    Standard flavor manifest overlay.
    S0423: ML Kit Translate is bundled in the standard base, so its AAR-merged MlKitInitProvider +
    MlKitComponentDiscoveryService run normally (no on-demand DFM, nothing to strip).

    S0418 screencapture (edge-gesture overlay + MediaProjection FGS) is NOT declared here: the
    SPECIAL_USE / SYSTEM_ALERT_WINDOW declarations are a Google Play review risk, so the feature
    stays noLegal-only for now (the src/screenCapture* source sets are not mounted into this flavor).
-->
<manifest xmlns:android="http://schemas.android.com/apk/res/android" />
