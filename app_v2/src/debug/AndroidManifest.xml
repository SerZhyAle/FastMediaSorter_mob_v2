<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application>
        <activity
            android:name=".ui.debug.CrashActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:theme="@style/Theme.FastMediaSorter" />

        <activity
            android:name=".ui.debug.DebugActivity"
            android:exported="false"
            android:theme="@style/Theme.FastMediaSorter" />
        <!-- S1986: opens the camera host in a named mode for the host-side WYSIWYG sweep. Exported
             on purpose - the sender is `adb shell am broadcast`, which is another uid. Debug only. -->
        <receiver
            android:name=".core.debug.CameraTestOpenReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.sza.fastmediasorter.debug.CAMERA_TEST_OPEN" />
            </intent-filter>
        </receiver>
        <!-- S2380: sets a colour theme on the running process for the host-side UI sweep. Exported
             for CAMERA_TEST_OPEN's reason - the sender is `adb shell am broadcast`, which is another
             uid, and a non-exported receiver is skipped for it. Debug only. -->
        <receiver
            android:name=".core.debug.ThemeTestReceiver"
            android:exported="true">
            <intent-filter>
                <action android:name="com.sza.fastmediasorter.debug.THEME_TEST_SET" />
            </intent-filter>
        </receiver>

    </application>

</manifest>