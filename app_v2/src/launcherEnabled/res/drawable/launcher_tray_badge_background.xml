<?xml version="1.0" encoding="utf-8"?>
<!-- S2023: the plate a tray badge sits on. Without it a digit or a letter drawn straight over an 18dp
     glyph is unreadable wherever the glyph is light, which is exactly where the icon is busiest. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorSurface" />
    <corners android:radius="2dp" />
</shape>
